# 子系统 E 的第二刀：指标核心与查询（E2）

> **归属**：run `orca-dev-ad1e30c6`，2026-09-08 写下，观测锚点 **`a93b463`**
> （主题行 `docs(spec): append the erratum that says the guard table was measured, not designed`）。
> **本文是 E 的三份 spec 中的第一份**：E2（指标核心 ＋ 查询）→ E3（索引器 ＋ 查询库）→ E4（Web 面板）。
> 人 2026-09-08 裁：**三份全部 brainstorm 完再实施**；顺序不变。
> ⚠️ **本文不写任何 HEAD、不写发布状态** —— 提交本文就会改这些数。要判发布状态现跑 `git ls-remote`。

---

## 0. 本文定什么、不定什么

**定**：从 `.decisions/**` 与 `~/.orca` 算出 A′ §4.4 那两个指标（外加积压与审阅覆盖率）所需的
全部口径、模块边界、判据与变异、以及一条可执行的成功判据。

**不定**（各属后续两刀）：
- **不建 DB、不定索引库 schema** —— E3。
- **不做任何 Web／UI** —— E4。
- **不改台账 schema**（`src/ledger/**`）、**不改 `CORRECTION_FIELDS`**。
- **不动 `src/corrections/storeLock.ts`** —— 见 §9 第 3 项（已登记的相邻诊断）。

---

## 1. 现测事实（每条带命令，观测于 `a93b463`）

### 1.1 `src/` 里没有任何索引、查询或指标代码

`find src -type f -name '*.ts'` 给出 37 个文件，全部在 `corrections/`、`ledger/`、`scheduler/` 三档下，
**没有 `metrics/`，也没有任何 DB 依赖**。⇒ 本刀是新增，不是改造。

### 1.2 correction 行带 `projectKey`，**不带路径**

`cat src/corrections/schema.ts`：`projectKey` 是 git remote URL，且它自己的注释写着
**"It is an identity, not a path: local checkouts move between machines, so resolution happens at read time."**
⇒ *** **「怎么从 projectKey 找到本机那个 checkout」本来就被登记为读侧的问题，也就是本刀的问题。** ***

### 1.3 往 correction 行加字段**不是免费的**

`cat src/corrections/fields.ts`：`CORRECTION_FIELDS` 是 **correction id 与 fix run id 的唯一哈希输入**。
⇒ 加一个字段会让**同样内容的行在新旧版本下派生出不同 id**。
⇒ *** **审阅记录不能做成 correction 上的新字段。** ***

### 1.4 时间戳全是挂钟，且 `src/` 零平台分支

`grep -rn "toISOString\|Date.now\|performance.now\|hrtime" src/`：所有 `at` 都是 `new Date().toISOString()`。
`grep -rn "process.platform\|darwin\|linux\|uname" src/`：**只命中 `preflight.ts` 的两条注释**（描述 git 行为），
**没有一处代码分支**。⇒ 本刀必须保持这一点：**零平台分支**。

### 1.5 `now` 注入已经是本仓库的约定

`src/scheduler/graph.ts:199` 现测为 `now: () => string = () => new Date().toISOString()`。
⚠️ **行号会移动，引用前现测。** ⇒ 本刀照抄这个形状，不发明新的（Rule 11）。

### 1.6 两个 `kind` 词表**完全不同**

`cat src/ledger/types.ts`：`DECISION_KINDS` ＝ dependency／interface／scheduling／abandon／criteria／boundary／reconcile。
`cat src/corrections/schema.ts`：`CORRECTION_KINDS` ＝ wrong／not_my_taste／stale。
⇒ **A′ §4.4 那句「按 `kind` 切分」有歧义。** 见 §3.1。

---

## 2. 数据流与边界

| | |
|---|---|
| **读** | ① 各目标仓库 `.decisions/*.jsonl`（默认跳过 `archive/`，`--all` 才扫全 —— A′ §3.7.1 已定）② `~/.orca` corrections store ③ reviews 表（本刀新增）④ repos 注册表（本刀新增） |
| **写** | *** **只写 `~/.orca` 下的 reviews 表与 repos 注册表。任何目标仓库一个字节都不写。** *** ⚠️ **两张表由谁写不一样，见 §2.2：注册表由本刀接进 `orca run`／`orca correct`（⇒ 本刀会改这两个既有子命令）；reviews 表本刀【只提供读写函数，自己一条都不产生】，产生它的是 E4。** |
| **算** | 纠正率、修复率、积压、审阅覆盖率（两条曲线） |
| **出口** | 纯函数层 ＋ `orca metrics` 子命令 |

**为什么一个字节都不写进目标仓库**：A′ §4.1 —— 「面板若直接写进仓库，就要求一个 Web 应用对**所有**仓库
持有提交权。**安全上不给。**」该节的表把 `corrections` 的作者定为「人（面板）」、真相源定为 DB。
⇒ **审阅记录与仓库注册表都属于人那一侧，落 DB 侧，永不进 git。**

### 2.1 仓库发现：扫描为主，注册表补根目录外（人裁，2026-09-08）

纠正率的分母是各目标仓库里的 `decision` 条数，而 corrections 是全局的、只带 `projectKey`（§1.2）。
⇒ 必须先知道「去哪些仓库数」。

**两条机制各管一格，不互相替代**（与 spec `2026-09-06-…` §7.1 同形）：

| 机制 | 管哪一格 |
|---|---|
| **扫 `--root <dir>`**：找带 `.decisions/` 的 git 仓库，用 `remote.origin.url` 算 `projectKey` 对号 | 「人忘了报的仓库」，尤其**有决策、零纠正的乖仓库** |
| **`~/.orca/repos.jsonl` 注册表**：`orca run` / `orca correct` 成功时记下 `projectKey → 绝对路径` | 「根目录之外的仓库」 |
| 🔴 **完整性闸门**：corrections store 里出现、但两条机制都对不上号的 `projectKey` | *** **点名拒绝，绝不静默略过** *** |

⚠️ **为什么闸门必不可少**：漏一个仓库会让分母变小 ⇒ **纠正率静默变高** ⇒ 看起来像「agent 质量变差」。
*** **最毒的一格是「有决策、零纠正」的乖仓库被漏掉 —— 指标恰好朝错误方向动，而没有任何东西会说。** ***

### 2.2 两个新写入方（**Rule 17 具名登记**）

| 路径 | 谁触发 | 失败留什么残留 |
|---|---|---|
| `~/.orca/reviews.jsonl` | **E4 的面板**（本刀只定形状与读写，不产生记录） | 一条审阅记录没落 —— **不影响任何台账，也不影响两个率**，只让覆盖率偏低 |
| `~/.orca/repos.jsonl` | `orca run` / `orca correct` 成功时 | 少一个根目录外的仓库 —— **会被 §2.1 的完整性闸门点名**，不会静默 |

**两者沿用 corrections 的全部纪律**：目录 `0700`／文件 `0600` **显式给定、不从 umask 继承**；
**已存在文件的 mode 不改**；路径一律经 `correctionsDir(env)` 解析，**判据全部走 `ORCA_CORRECTIONS_DIR` 改道**。
⚠️ *** **一条会写进使用者真实 `~/.orca` 的判据是不可接受的，哪怕它只写一行**（CLAUDE.md Rule 17）。 ***

---

## 3. 指标的精确定义

### 3.1 🔴 「按 kind 切分」是**哪个** kind —— 本刀的裁法

A′ §4.4 只写「按 `kind`、按项目、按时间切分」，而两个 kind 词表毫不相干（§1.6）。
⇒ **两个都要，但它们回答的不是同一个问题，必须分开命名，不许都叫「按 kind」。**

| 切法 | 是什么 | 回答什么 |
|---|---|---|
| 按 **`decision.kind`** 切纠正率 | **真的率**：分子经 `decisionId` 关联回原决策取它的 kind，分子分母同维度 | *** 「agent 在哪一类决策上更容易被纠正」*** —— 这才是 **A′ §4.4** 说的「决策质量」 |
| 按 **`correction.kind`** 拆分子 | **构成，不是率**（分母没有这个维度） | 「被纠正的那些里，事实错 / 口味 / 过期各占多少」 |

⚠️ **输出里两者必须用不同的字段名**，且构成那一组**不许除以分母** —— 见 §5 变异 10。

### 3.2 🔴 `stale` 默认出分子 —— 并给 A′ 追 ERRATUM 3

A′ §4.4 说纠正率量「**agent 的决策质量**」，分子是 corrections 条数。
可 A′ §4.2 明写 `stale` ＝「当时对，现在不对 → 什么都不训练，重做即可」。
⇒ *** **一条 `stale` 不是 agent 出错。算进分子，指标就在把「世界变了」记成「agent 变差了」。** ***

**裁法**：默认分子**排除 `stale`**，同时把**含 `stale` 的总数**一并输出，两个数都命名、都给。

⚠️ 这与 **A′ §4.4** 的字面冲突 ⇒ **给 A′（`2026-08-29-decision-ledger-design.md`）追加 `ERRATUM 3`**，
**不就地改**（该文件末尾已有 ERRATUM 1／2 的先例）。**这是本刀实施计划里的一个任务，不是可选项。**

### 3.3 时间口径：cohort ＋ 单报积压（人裁，2026-09-08）

**两个指标都按「被评价的那个对象」的时间分桶**：

| 指标 | 分桶用的时钟 | 桶的语义 |
|---|---|---|
| **纠正率** | `decision.at` | 「这一段时间做的决策里，有多少被纠正了」 |
| **修复率** | `correction.at` | 「这一段时间提的纠正里，有多少最终被修掉了」 |

⚠️ *** **cohort 桶的历史数字会随时间回填上升** *** —— 一月提的纠正三月被修，一月那桶的修复率就变了。
**这必须写进输出**（一个显式字段说明该桶仍在回填），否则人拿两次查询的同一个桶对比会以为出了 bug。

**为什么不按事件发生时间分桶**：分子分母不同源 —— 三月修掉一月的纠正，三月桶分子 +1、分母却是三月新提的纠正
⇒ *** **比值能超过 100%，它根本不是一个「率」。** ***

**为什么另外单报积压**：A′ §4.4 自己说修复率是用来发现「人看得见却没人管的积压」的
⇒ *** **它真正想量的是积压，不是率。** *** 积压 ＝ **未被任何 `overturned` 关联的 corrections 条数 ＋ 最老一条的年龄**。

### 3.4 边界情形（全部机械可判，无一项留判断题）

| 情形 | 怎么算 |
|---|---|
| 分母取哪些行 | 只 `ev === "decision"`。**`validateLine` 判 `rejected` 的行不进分母**（它不是一条合法决策）；判 `downgraded` 的**进分母**（记录合法，只是不许 agent 拍 —— A′ §1.1） |
| 一条 decision 被纠正多次（`--again`） | **按 corrections 条数计**（A′ §4.4 字面）⇒ 率**可以超过 100%**。输出里明说，**不夹带解释性折算** |
| correction 指向的 decision 扫不到 | *** **点名拒绝，不静默丢** *** —— 报「N 条纠正指向本次扫描范围外的决策」并给出 `projectKey` |
| 修复率的分子 | 从台账数 `ev === "overturned"` 的行，经 **`correctionId`** 关联回 corrections。**依据 A′ ERRATUM 1**：`overturned.id` 是被推翻的 decision 的 id，`overturned.correctionId` 才是 corrections 的主键 |
| 积压年龄为负 | *** **点名拒绝**（时钟偏斜），不打印负年龄 *** —— 见 §4.2 |

### 3.5 审阅覆盖率：两条曲线，分开展示（人裁，2026-09-08）

A′ §4.4：「纠正率下降**也可能**是因为人看得少了 ⇒ 必须与**面板的人工审阅覆盖率**同时读，单看纠正率会自欺。」
而**「人看过哪条决策」今天没有任何地方记着** —— 台账里没有，corrections 里没有。

**reviews 行的形状**（住 `~/.orca/reviews.jsonl`，**不是** corrections 的一种 kind）：

| 字段 | 说明 |
|---|---|
| `decisionId` | 看的是哪一条 |
| `projectKey` | 哪个项目（与 corrections 同一个键，跨项目聚合不需要新键 —— A′ §4.3） |
| `action` | *** **`expanded`（在面板上展开了详情）｜ `handled`（显式处置：记了 correction，或显式点了「同意」）** *** |
| `by` / `at` | 谁、什么时候（挂钟 ISO 8601） |

**为什么不塞进 corrections**：`CORRECTION_KINDS` 是「**人否定了什么**」的分类；「看过但没意见」不是一种纠正。
塞进去会**直接污染纠正率的分子** —— 而 §3.1／§3.2 花了两节就为把这些分开。
**为什么不做成 correction 的新字段**：§1.3，那会改 id 派生。

**两条曲线各自的覆盖率** ＝ 有对应 `action` 记录的**高位决策**数 ／ 高位决策总数。
「高位」由 A′ §3.6 的 `scope` / `kind` 排序键定，**本刀只消费它，不重新定义**。

---

## 4. 时钟

### 4.1 挂钟管时间戳与年龄；单调钟只管进程内时长

*** **积压年龄必须用挂钟，绝不能用单调钟。** *** 「这条纠正积压了 9 天」**必须把笔记本合盖的那几天算进去**
—— 那正是积压的定义。而单调钟在 macOS 与 Linux 上**都不跨睡眠**（Linux 要跨睡眠得换 `CLOCK_BOOTTIME`，
**那是 Linux 专有，直接破坏跨平台**）。⇒ 拿单调钟算年龄会**系统性少算**。

⚠️ *** **本刀【没有实测】两个平台单调钟的睡眠语义**（那需要真的让机器睡）。所以设计上直接绕开它：
凡是必须跨睡眠的量，一律不依赖单调钟。** *** 这比依赖一条没量过的平台断言更稳（Rule 14）。

⚠️ **这是「一条先例不要外推」的一个实例**：ccmem 刚为 `monotonic_ms` 立了先例，
但它解决的是「**一次调用是不是挂住了**」—— 那是**同一进程内的时长**。外推到「跨机器、跨天的年龄」上是错的。

**唯一用单调钟的地方**：进程内时长（如扫描耗时）⇒ `performance.now()`，Node 内置，两平台一致，**零平台分支**（§1.4）。

### 4.2 真正的跨机器危险是时钟偏斜，不是 API

corrections 可能写在 A 机、decisions 写在 B 机；年龄 ＝ `本机 now − 别机写的 at` ⇒ **可以是负数**。
⇒ *** **点名拒绝，不打印负年龄。** *** 与 A′ §3.7「非 git 目录那一档确实弱，不掩饰」同一种诚实。

### 4.3 `now` 注入

`compute` 层**零 fs、零 git、零时钟**，`now` 由调用方注入，形状照抄 `graph.ts`（§1.5）。
⇒ *** **积压年龄、cohort 分桶、时钟偏斜拒绝，三者的判据都能给定死的 `now`，不靠真实时间。** ***

---

## 5. 模块边界

| 模块 | 干什么 | 依赖 |
|---|---|---|
| `src/metrics/repos.ts` | 仓库发现：扫 `--root` ＋ 读注册表 ＋ 算 `projectKey` 对号 | fs、git |
| `src/metrics/registry.ts` | `~/.orca/repos.jsonl` 的读与追加 | fs、锁 |
| `src/metrics/reviews.ts` | `~/.orca/reviews.jsonl` 的读与追加 | fs、锁 |
| `src/metrics/collect.ts` | 台账行 ＋ corrections ＋ reviews → 一组扁平观测 | 上面三个 |
| 🔴 `src/metrics/compute.ts` | *** **观测集 → 指标对象。零 fs、零时钟、零 git。** *** | 无 |
| `src/metrics/report.ts` | 渲染（JSON ＋ 人读表格） | compute |
| `src/cli.ts` | `orca metrics` 子命令（第六个） | 全部 |

**为什么 `compute.ts` 必须是纯的**：它是唯一被 E3（灌库）与 E4（展示）**共同消费**的东西。
一个带 fs 与时钟的指标层，会让 E3 和 E4 各自复制一份口径 —— 而口径漂移正是 A′ §4.4 反复在防的。

### 5.1 🔴 读侧不取锁

`orca correct` 写 corrections 是**持锁追加**的。读侧若也取锁，**面板每刷新一次就阻塞一次 `orca correct`**
—— 而 spec `2026-09-06-…` §14.3 花了整整一节论证「只记模式不取任何锁，好让一轮在飞时人照样能记」。
*** **读侧取锁会把那个成果吃掉一半。** ***

不取锁的代价是可能读到不完整的最后一行。**做成一条机械判别，不留判断题**：

| parse 失败在 | 判 | 理由 |
|---|---|---|
| **最后一行** | **点名报告并继续**：「最后一行不完整（很可能有一次写入正在进行）—— 本次计入 N 行」 | 只追加文件的并发读，这是唯一可能的撕裂位置 |
| **非最后一行** | *** **硬拒绝** *** | 那不是竞态，那是真损坏 —— 只追加的台账，损坏必须吵 |

⚠️ **本刀没有实测 POSIX `O_APPEND` 小写入的原子性，所以设计上不依赖它**：不管原子不原子，上面两条都成立。

---

## 6. 判据与变异（**每新增一个分支，点名一条删掉【它自己】的变异**）

| # | 分支 | 删掉**它自己**的变异 | 期望 |
|---|---|---|---|
| 1 | `stale` 出分子（§3.2） | 删掉排除条件 | 纠正率数字变。*** **判据钉【字面数字】，不钉 `compute.ts` 里的常量** *** |
| 2 | `rejected` 行不进分母（§3.4） | 删掉过滤 | 分母变大 |
| 3 | `downgraded` 行**进**分母（§3.4） | ⚠️ **「什么都不做」的分支，删不掉** | ⇒ 配**正向观测**：夹具放一条 downgraded，钉分母的字面数 |
| 4 | cohort 分桶（§3.3） | 换成按事件时间分桶 | 桶归属变 |
| 5 | 负年龄拒绝（§4.2） | ⚠️ **删掉守卫会让整个调用成功 ⇒ 消息断言先红，「什么都没输出」那条从没被执行** | ⇒ *** **用故障注入**：保住抛错与消息，只把它挪到输出之后 *** |
| 6a | 最后一行不完整 ⇒ 容忍并报告（§5.1） | 放宽成「任意行都容忍」 | 中间损坏被吞 ⇒ 红 |
| 6b | 非最后一行损坏 ⇒ 硬拒（§5.1） | 收紧成「一律拒」 | 并发读直接崩 ⇒ 红 |
| 7 | 对不上号的 projectKey 点名拒绝（§2.1） | 改成静默略过 | 完整性闸门失效 ⇒ 红 |
| 8 | **读侧不取锁**（§5.1） | ⚠️ **「不做某事」，同样删不掉** | ⇒ *** **负向对照判据**：`orca correct` 持锁在飞时 `orca metrics` **必须仍然成功** *** |
| 9 | `now` 注入（§4.3） | 改回直接 `new Date()` | 定死年龄的判据变红 |
| 10 | 两个 kind 分开（§3.1） | 把 `correction.kind` 的构成也去除以分母 | 数字变 |

⚠️ **第 3、8 两条最容易骗过自己** —— 它们钉的是「什么都没发生」。
*** **钉「什么都没发生」的判据必须配一条正向观测，否则它是空的。** ***

⚠️ **变异纪律**（照抄前几轮的血教训）：变异只在 `git clone --local` 副本里做，主工作树零触碰；
副本**只克隆已提交状态**，要变异未提交的改动必须先 `cat` 进副本并 `diff` 证明逐字节相同；
新副本**没有 `node_modules`** ⇒ `ln -s <主仓库>/node_modules <副本>/node_modules` 再走 `./node_modules/.bin/vitest`，
否则 `npx vitest` 死在 config 导入上给你一个**不是判据变红**的 `RC=1`。

---

## 7. 成功判据（Rule 4：**一条能跑出 0／非 0 的命令**）

```
在 scratchpad 下造两个一次性 git 仓库（各有已知条数的 decision，含一条 rejected、一条 downgraded，
其中一条 decision 被一条真的 overturned 推翻），
用 ORCA_CORRECTIONS_DIR 指向一次性 corrections 目录（已知条数的 correction，含一条 stale），
然后：
    orca metrics --root <tmp> --json > out.json
    diff out.json <逐字节写死的 golden.json>          # 必须 exit 0
并且三条 fail-loud 各自 exit 非 0：
    对不上号的 projectKey ／ 负年龄（注入未来时刻的 at）／ 中间行损坏
```

⚠️ **收尾必须现测 `ls ~/.orca` 仍不存在** —— 与 E1 一样，这是「没有一条判据碰过使用者真实用户数据」的证明。

---

## 8. 与后两刀的接口（**E3／E4 各自另开 spec，本节只声明会被拿走什么**）

| | 子刀 | 从 E2 拿走 |
|---|---|---|
| **E3** | 索引器 ＋ 查询库 | `compute.ts` 的**指标对象形状**（DB schema 是它的函数）；`collect.ts` 的观测集形状；`archive/` 与 `--all` 的口径 |
| **E4** | Web 面板 | 同一个 `compute.ts`（**不许复制口径**）；reviews 表的写入口（§3.5）；A′ §3.6 的排序键 |

⚠️ *** **E3／E4 的 spec 建在【尚未实现】的 E2 之上。** *** 人 2026-09-08 明确选了这条路，风险已当面说明：
上一刀的实测是「**十一个任务里有六个是实施者揭发了计划的测试代码有问题**」⇒
**若 E2 的实施推翻了 E3／E4 的前提，按规矩给那两份追加具名 ERRATUM，不就地改。**

---

## 9. 已知边界与登记项（**本刀明确不做**）

1. **不建 DB** —— A′ §3.7 说「跨仓库查询很难做，而面板是核心交付物」，那正是 E3 存在的理由。
   本刀直接扫文件，**在今天的数据量下扫描买得到 DB 买得到的一切**，且能当天跑出真数字验收。
2. **`--root` 之外、且不在注册表里的仓库仍会被漏掉。** 完整性闸门只能发现「有纠正但找不到」的项目，
   *** **发现不了「有决策、零纠正、且不在两条机制覆盖内」的乖仓库。** *** **这是本刀未消除的代价，不掩饰。**
3. ✅ **已修，不再是登记项**（人 2026-09-08 具名授权「先把两条相邻诊断修了」）：
   `src/corrections/storeLock.ts` 原本用 `Date.now()` 算锁超时的 deadline —— 那是**用挂钟量时长**。
   提交主题行 `fix(storeLock): stop measuring an elapsed second with a clock that can be set`。
   ⚠️ **本文原句写的是「本刀不动手」，那是写下时为真、随后被人的授权推翻的；此处即为就地更正**
   （本文当时未发布，现测远端不含它 ⇒ 就地改合法）。
   *** **这条修复顺带产出了本刀 §4.1 那条设计的第一份实证**：在进程内，`Date.now()` 与
   `performance.now()` 不可区分，除非挂钟真的动 —— 而判据动不了挂钟。
   ⇒ **「换回挂钟」这个变异唯一可观测的形状，是「注入的时钟不再被问」。** ***
   实测：变异下**拒绝那条断言照绿**（变异体真的跑满 1000ms 才超时），红的是读数表 `[]`。
   ⇒ **E2 的 §6 变异表里凡是涉及时钟的，都要照这个形状写，不要只断言「拒绝了」。**
4. ✅ **已做，不再是实施计划里的任务**（同一次授权）：`src/corrections/schema.ts` 那句
   「there is no database, **no writer and no CLI subcommand** for it yet」现在为假
   （writer 是 `store.ts`／`correct.ts`，子命令是 `orca correct`）。
   该注释**已发布**（其最后一笔 `2b34435` 现测是远端 `main` 的祖先）⇒ 原句逐字保留，
   **在注释块末尾追加了具名 ERRATUM**。提交主题行
   `docs(corrections): erratum the comment that says nothing has been built yet`。
   ⚠️ **erratum 里唯一活下来的那半句「there is no database」指向的就是 E3** —— 本文 §8 已登记。
5. **审阅记录本刀只定形状与读写函数，自己一条都不产生**（与 §2 的「写」一格一致）—— 产生它的是 E4 的面板。
   ⇒ *** **在 E4 落地之前，审阅覆盖率永远是 0，而 A′ §4.4 要求它与纠正率同读。
   这段时间里纠正率必须带着「覆盖率无数据」的显式标注输出，不许裸给。** ***
6. **跨机共享文件系统下的 `~/.orca`** —— 与 corrections 的已知限制相同，本刀不扩大也不解决。

---

## 10. 边界声明

本刀**不放松**它所读取的任何目标仓库的规则（CLAUDE.md Rule 16）。
本刀**不写**任何目标仓库、**不取**任何仓库的 repo 锁、**不取** corrections 的 store 锁（§5.1）。
本刀新增的两个写入方**只写 `~/.orca`**，且全部经 `ORCA_CORRECTIONS_DIR` 可改道（Rule 17）。
