# Preflight conflict scan — execution driver plan

> **Attribution**: preflight review seat (read-only) for controller session `905e41ce`, 2026-09-25.
> Plan `docs/superpowers/plans/2026-09-25-execution-driver.md` (4886 lines, 11 Tasks) and spec `docs/superpowers/specs/2026-09-25-execution-driver-design.md` (§11 overrides earlier sections; D1–D21 are accepted rulings).
> Observed at Orca HEAD `e1f2bc9` (`docs(plan): task-by-task plan for the execution driver, …`). ccloop was read at its working tree. All line numbers are plan line numbers unless a file is named.
> Method: plan text read in full (Task 1–11), every cross-task import/call checked against the defining Task's code, and load-bearing anchors checked against the real code (python/sed line reads; no tests run).
> Verdict legend: **OK** = the two sides agree; **NOTE** = they agree, but there is a hazard worth knowing; **DEFECT** = a reviewer would flag this; **CONFLICT** = a name, type or signature mismatch.

## Part 1 — Task pairs that share a file or interface

| # | Tasks | Producer → consumer (exact names) | Verdict |
|---|---|---|---|
| P1 | T1 → T9 | T1 fake-codex `script` mode argv `[node, fake-codex.mjs, "script", marker, scriptPath]` (146, 437-442) → T9 adapter `command` (4163) and fake-codex path `resolve(dirname(ORCA_CCLOOP_BIN),"..","tests","fixtures","fake-codex.mjs")` (4162). The script keys `reconcile-a-b`/`reconcile-b-a` (4259) match real `reconcile.ts:361` `reconcile-${slug(a)}-${slug(b)}`. | OK |
| P2 | T1 → T9 | T1 C2 namespaced ref `refs/ccloop/<claim.runId>/attempts/<n>`, which the result repository reads (378, 431) → E1/R1 through the real ccloop build (T9 Step 1). | OK |
| P3 | T2 → T3 | `type WorkspaceMode = "worktree" \| "clone"` (1023) → `workspace.ts` import (1412) for `ensureWorkspace(targetRepo, mode, path, base, roots)`. | OK (T3's Consumes prose, 1225, omits it) |
| P4 | T2 → T4 | `DriveRecord` (806-822, 15 fields) → `newDrive` (2220-2226, the same 15 fields); `DriveStep` → `blockRun(..., step: DriveStep, ...)` (2192); `readWorkspaceSetting(store, repoId).workspaceMode` (1030) → `stepA1` (2238). | OK |
| P5 | T2 → T4 | `persistedRunSchema` adds states `start-pending/collected/landed/reconciling/blocked` plus `drive` (1078). T4 writes exactly these states (2252, 2318, 2418) and `blocked` via `blockRun`/`refuse`. `accepted` is kept for running (D3). | OK |
| P6 | T2 → T5 | `RESUME_STATE.D = "collected"` (827) → `stepD` guard `state === "collected"` (2758). `blockRun(...,"D", "repository-path")` (2762) resumes into D. | OK |
| P7 | T2 → T6 | `reconcileRecordSchema` (788-803, 13 fields) → `ReconcileRecord` literal in `beginReconcile` (3182-3185): same 13 fields. `resumeBlockedDriverRun` at R resets `spawning:false, pid:null` (847), which `reconcileNextAction` (3202) then reads as `"spawn"`. | OK |
| P8 | T2 → T7 | `RESUME_STATE.E = "landed"` → `stepE`'s `run.state === "landed"` (3594). `cleanedUp` field → D15 cleanup visit (3620-3627). | OK |
| P9 | T2 ↔ T8 (`src/control/stopIntent.ts`) | T2 narrows the target at `:132` and `:404` (1006-1007, inside `applyRecoveryRetry`). T8 edits `retryRun` (real `stopIntent.ts:444` `const resolved = blockers.resolved \|\| rearm !== null;`, which matches the precondition at 3915). The two edits don't overlap. `resumeBlockedDriverRun(store, runId): boolean` (836) → T8 import and call (3914, 3924). | OK |
| P10 | T2 → T9 | `WebServiceDeps.knownRepository?` (1093) and `TrustedControlConfig.resolveRepository(repoId): string` (1106-1115) → controlAssembly `knownRepository:` (4393) and `resolveRepository: (repoId) => config.resolveRepository(repoId)` (4418). `set-workspace-mode` at revision 0 → E2/W1 `raw()` (4232). | OK |
| P11 | T2 → T10 | `RepositoryWorkspaceV1` (web, 1160), `RunViewV1.blockedReason?` (1156), GET `/api/control/repositories/:repoId/workspace` (1120) and POST `…/workspace-mode` (1133-1141) → `fetchRepositoryWorkspace`/`workspaceModePath` (4634-4637) and `ControlGroupView` (4654). The ledger key `@repository:<repoId>` (956-958, 1138) → the web refusal `groupId` (4675). | NOTE: 4695 leaves the reducer's handling of a non-group `groupId` open ("stop and report") |
| P12 | T3 → T4 | `controlWorkspaceRoots`, `sourceDirOf`, `workspacePathOf`, `ensureWorkBranch`, `ensureWorkspace`, `commitAttempt`, `WorkspaceRoots` (1419-1511) → imports at 2093 and calls at 2238-2273 and 2407. Every arity and order matches. | OK |
| P13 | T3 → T5 | `QUIET_GIT`, `landingPathOf`, `removeOwnPath(targetRepo, roots, path)`, `revParse(repo, rev)`, `compareAndSwap(repo, ref, next, old)`, `workBranchRef`, `incomingRefOf` (1419-1519) → import at 2718 and calls at 2763-2787 (`compareAndSwap(targetRepo, branch, next, old)`). | OK |
| P14 | T3 → T6 | `conflictPathOf`, `reconcileRunsDirOf` (1435-1436) → import at 3112 and calls at 3164/3175. | OK |
| P15 | T3 → T7 | `cleanupRunWorkspace(targetRepo, roots, runId, workspacePath)` (1522) → `stepE` (3623). | OK |
| P16 | T3 → T9 | `controlWorkspaceRoots(store.stateDir)` → 4416. `stateDir` is real (`store.ts:37`). | OK |
| P17 | T4 ↔ T5 (`executionDriver.ts`) | T4 exports `blockRun`, `write`, `readDriverRun`, `saveDriverRun`, `groupRepoId`, `ExecutionDriverDeps{beforeCas?, crash?}` (2118-2202) → T5 import (2717). T5 adds `import { stepD }` and `case "collected"` after `case "accepted"` in `advance` (2794-2797). The anchor exists at 2459. | OK. NOTE: this creates a circular import executionDriver ↔ driverLanding. It is safe because neither module reads the other's bindings at load time. |
| P18 | T4 ↔ T6 (`executionDriver.ts`) | `DriverContext`, `DriverRun{taskId}`, `describeError`, `ccloopBin`, `adapterConfigPath` (2118-2158) → T6 (3111, 3263-3268). T6 rewrites the import to `{ stepD, stepR }` and adds `case "reconciling"` after `case "collected"`, dropping `void context;` (3335-3338). T4's `FAKE_CCLOOP_RUN` (1740) points at the file T6 creates (2867). | OK. The forward reference is acknowledged at 1601. |
| P19 | T4 ↔ T7 (`executionDriver.ts`) | `nextClaimableTask` export (2025) → `replenishStartWakes` (3650). `driverRunIds` already visits `settled && !cleanedUp` (2434), as D15/`stepE` need. T7 adds `case "landed": case "settled"` after `case "reconciling"` (3670); the anchor comes from T6. T7's import block (3565-3572) re-imports modules T4 already imports (`./budget.js`, `./webDispatch.js`, `./archive.js`, `./executionPort.js`, `./workspace.js`); 3575 says to merge them. | OK (merge per 3575) |
| P20 | T4 ↔ T7 (`pass`) | T7 puts `replenishStartWakes` at the head of `pass` (3676-3682), with its own `panel-draining` ⇒ `return false`. T4 test 12 (1939-1946) expects `round()===false` while draining, and that still holds. | OK |
| P21 | T4 → T8 | `isWebWorkRun(store, runId)` (2068) → `recovery.ts` (3878, 3889) and `controlLifecycle.ts` (3895, 3908). The fixtures `driverHarness`/`FakeBehaviour` (1626, 1756) → `driverRecovery.test.ts`. | OK |
| P22 | T4 → T9 | `createExecutionDriver`, `ExecutionDriver{round,kick,start,stop,crashed}`, `CrashPoint` (5 values: 2108) → controlAssembly (4372, 4407, 4420, 4431-4449) and R1 `it.each` (4330, the same 5 literals). The crash hooks sit at 2274, 2355, 2401, 2783 and 3605, one per value. | OK |
| P23 | T5 ↔ T6 (`driverLanding.ts`) | T6 replaces T5's merge-failure `blockRun(...,"merge-conflict")` with `removeOwnPath; return beginReconcile(deps, runId, targetRepo, old)` (3118-3123). No T5 criterion or mutation pins `merge-conflict` (T5-M1..M5 at 2839-2843), so nothing goes stale. T6's new import block (3097-3115) keeps everything T5's code uses (`join`, `ControlError`, `ORCA_IDENTITY`, `revParse`). | OK |
| P24 | T5/T6 → T7 | `markLanded` → `state:"landed"` and `landedCommit` (2740-2746). `finishReconcile` → `landed`, with `landedCommit` = the rebuilt merge (3321). `stepE` then writes acceptance with `commit: drive.landedCommit` (3602). The reconciled `^2` is the incoming attempt (real `reconcile.ts:453-456`), which is what RC1 asserts at 2948. | OK |
| P25 | T6 → T9 | The reconcile run is spawned with `deps.adapterConfigPath` = `ORCA_CCLOOP_ADAPTER_CONFIG` (4419), i.e. the E1 script adapter. `runTask`'s loop dir leaf = `reconcile-<runId>`, and `latestAttemptSha(cloneDirOf(workdir), reconcileRunId)` (3259) matches real `ccloopRunner.ts:68,82,236-258`. E1 counts `calls()` = 12 (4 runs × plan/execute/verify). | OK |
| P26 | T7 → T8 | T8 test 7 (3850) drives a retried run to `settled`/`cleanedUp`, so it depends on T7's `stepE`. The dependency order T7 → T8 holds (128). | OK |
| P27 | T8 → T9 | `recoverControl(..., options:{driverOwnsWebRuns?})` (3881) → `{ driverOwnsWebRuns: driver !== null }` (4431). `PanelShutdownDeps.exemptDriverRuns?` (3897-3902) → `exemptDriverRuns: driver !== null` (4444). | OK |
| P28 | T2–T9 → T11 | Stated criterion counts: driveRecord 15, workspaceSettings 7, workspaceModeApi 2, workspace 11, executionDriver 17, driverLanding 4, driverReconcile 14, driverSettle 7, driverRecovery 7, controlAssemblyDriver 2, E2E 9 → `EXPECTED` (4757-4768). All eleven agree. | OK |
| P29 | T10 → T11 | `web/tests/workspaceMode.test.tsx` (3 criteria, 4578) is **absent from `EXPECTED`** (4757-4768), although 4755 says "every new criterion file of this round". It is still executed: `npm run --ws check`, which is the `ws-check` gate, is `tsc && vitest run` in `web/package.json`. Its count is simply not pinned. | DEFECT (minor) |
| P30 | T1 → T11 | The ccloop gates (4812-4815) run `ccloop-test`, but 4819 gives no RC rule for it. Only `known-reds` over its json is judged. | NOTE |
| P31 | T4 ↔ T7 (`executionDriver.ts`, `budget.ts`) | T7's `stepE` (3620-3628) revisits `state==="settled" && !cleanedUp` runs and calls `await cleanupRunWorkspace(...)` at 3622 **outside any try/catch of its own**. `pass()`'s generic catch (2472-2489, written by T4) is the only handler in scope; on a thrown error it calls `blockRun(deps, runId, stepOf(run), describeError(error))` (2484), and for a `"settled"` run `stepOf` (2440-2450) falls to `default: return "E"`. `blockRun` (2192-2200) unconditionally sets `run.state="blocked"` and calls `saveDriverRun`→real `saveRun` (`budget.ts:24-32`, verified in real code): that function only writes the `body` column, never touches `active`. But T7's Web settle path releases budget through real `terminaliseRun` (`stopIntent.ts:648-650`, verified), which calls `saveRunBody(store, settled, false)` — i.e. it sets DB column `active=0` at settle, before cleanup ever runs. So a `cleanupRunWorkspace` failure on an already-settled run leaves the row at `active=0`, `state="blocked"`. Verified against real `src/panel/controlViews.ts:465-468`: `(Number(row.active)===1)===terminal` — `"blocked"` is not in the terminal display list (`:464`), so `terminal=false`, and `active=0` also gives `false`; `false===false` ⇒ `true` ⇒ throws `blocked(\`run-state:${runId}\`)` (`:141-143`), which aborts `runViews()` for **every** run in the group, not just this one. No task's test suite (T4's, T7's, or T9's E1/R1) drives a cleanup failure on a settled run, so nothing in the plan as written would catch this before it reached a real deployment. | CONFLICT |
| P32 | T9 self (`executionDriverE2E.test.ts`) | Task 9's own "Interfaces" prose (4012) says the E2E file is skipped with `it.skipIf(!ORCA_CCLOOP_BIN)`; the actual code block (4253) instead wraps the whole suite in `describe.skipIf(!realBinary)`, a different guard on a differently-named variable. The code is what an implementer would actually run, so this is not a build-breaking defect, but it is a real disagreement between what Task 9 tells the reader to expect and what it hands the reader to type. | NOTE (prose/code disagree) |
| P33 | T9 self (`controlAssembly.ts`) | `shutdown()`'s `shutdownRun` IIFE (4441-4446) already does `await driver?.stop(); await inFlight; ...`. `close()` (4449) is written as `stopTimer(); void driver?.stop(); store.close();`, adding a second, un-awaited `driver?.stop()` call on top of whatever `shutdown()` already did. The real, pre-plan `close()` (`src/panel/controlAssembly.ts:249`) only does `stopTimer(); store.close();` — it has never before had to be idempotent against an async stop. `ExecutionDriver.stop()` is documented (2153-2154) to wait for the round in flight, so calling it twice is very likely harmless, but no task's mutation table or test exercises `close()` after `shutdown()` to prove it (T9-M4/M5 at 4498-4499 admit exactly this gap for a related line). | NOTE (unverified duplicate call) |

## Part 2 — Per-task self-consistency

| # | Task | Tests vs code, imports, literals | Files vs commit | Verdict |
|---|---|---|---|---|
| S1 | T1 (132-498) | All 7 criteria trace to the Step 4 code. `namespacedAttemptRefName(attempt, runId)` (378) is type-correct: in real ccloop `resultRepository.ts:70`, `attempt` is the path `join(runDir,"worktrees","attempt-N")`, not a number (an extraction pass flagged this; rejected). The fake-codex insertion point is after real line 24, and argv[4] is the script path. | All 7 files are in `git add` (464). | OK |
| S2 | T2 (501-1217) | All 24 criteria trace (the literal table at 603 equals `RESUME_STATE` at 826-828 without deriving it from `RESUME_STATE`). `runViewSchema` omits `start-pending` on purpose; `displayRunState` maps it to `starting` (1082). The fixture exports are to be confirmed at implementation time (755, self-flagged). | 16 files, all staged (1181). | OK, but see D2 |
| S3 | T3 (1218-1579) | 11 criteria trace. The `%an <%ae>` literal `orca <orca@invalid>` matches the real `gitExec.ts` `ORCA_IDENTITY`. | 2 files staged (1545). | OK |
| S4 | T4 (1581-2591) | 17 criteria trace (R2: accept 1 / inspect 10; forget-first-accept: accept 2 / inspect 1; wrong-config leaves `executionId:null` because the block precedes assignment at 2304-2305). The Consumes prose (1590) omits many imports that the code does import (2078-2100); not a defect in the code. | 6 files staged (2545). | OK, but see D4 and D5 |
| S5 | T5 (2592-2846) | 4 criteria trace. `join` is used (2772: `join(drive.sourceDir,"repo")`); the extraction pass's "unused import" claim is rejected. | 3 files staged (2816). | OK |
| S6 | T6 (2847-3398) | 14 criteria trace (27 = 10+10+7; `markers-remaining:shared.txt`; `reconcile-other-side:0`). The "orphan" test (3035-3045) is not racy: `until` stops on the round that reaches `reconciling`, and `stepD` never also runs `stepR` in the same pass, so nothing has spawned. The test itself asserts 0 spawns (3043). Four mutations are self-declared not-red (3382-3395). | 5 files staged (3357). | OK |
| S7 | T7 (3399-3739) | 7 criteria trace. `"arms nothing for a group nobody started"` (3525) is green before the change (3536 says so) and only goes red under T7-M6. | 3 files staged (3700). | NOTE (see D6) |
| S8 | T8 (3740-4006) | 7 criteria trace. G2, G4 and test 6 (3794, 3816, 3841) are green before the change (3869) and only go red under T8-M2/M4/M6. Step 5 shows two versions of the same line (see D1). | Two commits, code then spec erratum (3963-3967): all 5 files. | DEFECT (D1) |
| S9 | T9 (4007-4505) | 2+9 criteria. The T1 regex over the terminal literal is pinned at 4354 as "stop and report, don't widen". T9-M4/M5/M7 are predicted not-red (4498-4501). | 3 files staged (4469). | OK |
| S10 | T10 (4506-4741) | 3 criteria trace. SSR whitespace (4628) and reducer handling (4695) are self-flagged "stop and report". | 6 files staged (4714). | OK |
| S11 | T11 (4742-4886) | The judge's red-proof predictions match its logic (11 missing → 11 lines, RC 1). `progress.md` gets its own `git add -f` (4837, 4850). | Only `progress.md` is committed. | DEFECT (P29), NOTE (P30) |

## Part 3 — Defects a code reviewer would flag

| # | Where | What | Class |
|---|---|---|---|
| D1 | T8 Step 5: 3918 vs 3924-3926 | The plan shows a first `retryRun` line, `resolved = blockers.resolved \|\| rearm !== null \|\| resumeBlockedDriverRun(...)`, which short-circuits past the side effect. Only after that does it give the corrected two-line form. An implementer who applies the first block ships the bug. The fix is to delete 3918 from the plan. | plan-text hazard |
| D2 | T2 Step 6: 978-980 vs 989-991 | `preflightWebCommand` and `applyWebCommand` get the same `groupRow` / `currentCommandRevision` block, word for word. | verbatim duplicated logic |
| D3 | T5 2767-2769 + 2786-2788 vs T6 3302-3304 + 3311-3313 | "`landingPathOf` → `removeOwnPath` → `worktree add --detach` → … → `finally removeOwnPath`" is duplicated between `stepD` and `finishReconcile`; only `old` vs `record.old` differs. | verbatim duplicated logic |
| D4 | T4 2417 vs 2418 | The comment says "an empty result is not landed", yet the next line sets `state = "landed"` (with `landedCommit:null`, per D14). The comment contradicts the code, and the criterion at 1993-2002 encodes the code. | comment/code contradiction |
| D5 | T4 2306-2307 vs 2313-2314 | `run.state = "accepted"; run.drive = { ...drive, inspectUnknown: 0 };` is repeated in the `accepted` and `stopped` branches. | trivial duplication |
| D6 | T7 3525; T8 3794, 3816, 3841 | These criteria cannot be red before the change (the plan says so at 3536 and 3869). Under CLAUDE.md Rule 9 they count as criteria only once T7-M6 and T8-M2/M4/M6 are actually seen red in the mutation step. | criteria not yet able to fail |
| D7 | T4 2472-2489 (`pass`) ↔ T7 3620-3628 (`stepE` cleanup revisit) ↔ real `budget.ts:24-32`, `stopIntent.ts:648-650`, `controlViews.ts:465-468` | See P31. A `cleanupRunWorkspace` failure on an already-`settled` (`active=0`) run gets `blockRun`'d by T4's generic catch, which writes `state="blocked"` but never restores `active`. That `active=0`/`state="blocked"` combination trips the real `run-state:<runId>` guard in `controlViews.ts` and aborts the read view for the **entire group**, not just the failed run. No task's tests exercise a cleanup failure on a settled run. | real cross-task defect (verified against real code) |
| — | Checked, none found | No assertion placed before the call that reads back its own input. No `expect(true)`-style empty assertion. No file created twice. No task modifies an earlier task's code in a way that contradicts it. No consumed function lacks a producer. | — |

**Rows: 33 (pairs) + 11 (per-task) + 7 (defects) = 51.**

> Addendum (preflight coordinator, after the initial 47-row pass): rows P31-P33 and D7 above were added after independently re-deriving and verifying, against the real repository code (`src/control/budget.ts`, `src/control/stopIntent.ts`, `src/panel/controlViews.ts`), a cross-task hazard between Task 4's generic error handler and Task 7's settle path that four earlier extraction passes had described only in part or not at all. P31/D7 is the one finding in this file classified **CONFLICT** rather than NOTE/DEFECT/OK — it is a real, verified interaction bug, not a hypothetical.
