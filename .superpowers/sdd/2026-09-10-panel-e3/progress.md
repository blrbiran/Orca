# SDD ledger — plan: docs/superpowers/plans/2026-09-10-panel-e3.md

Owner: run `orca-dev-6354277a` (session 6354277a), started 2026-09-10 22:38 +0800 on main @ a801200.

## Start-of-session measurements (commands redirected to files, read back whole)

- `git ls-remote origin refs/heads/main` -> 2aadb33 (`docs(handoff): correct the registration that said the sister repos were left alone`); local main 4 commits ahead (b71093a, 400035c, fac3043, a801200). Handoff said 6; measured 4.
- `rtk proxy npm run verify` -> VERIFY_RC=0; whole repo 82 files / 467 tests; verify:scheduler 51 / 167.
- `git status --porcelain -z` -> 0 bytes; one worktree.
- `ls ~/.orca`, `ls src/panel`, `ls -d web` -> all "No such file or directory".

## Deviations from the skill (CLAUDE.md wins, Rule 11)

- No isolated worktree: CLAUDE.md Rule 15 makes deleting a worktree need human sign-off; every prior round landed local commits on main. Human asked for this execution on 2026-09-10.
- Workspace is NOT deleted at the end: this repo keeps `.superpowers/sdd/**` evidence via `git add -f`.
- Mutations are run by a separate verifier subagent after review, not only by the implementer (handoff item 9: red-count acceptance moves to after implementation).

## Human rulings this session (2026-09-10, AskUserQuestion)

- H1: AUTHORISED by name — change existing production code `src/corrections/correct.ts` + `src/corrections/record.ts`: move `correctionRowFrom` into a shared single construction point, `at` injectable as a function parameter; no `--at` CLI flag.
- H2: spec §3.1 gets NO ERRATUM. Controller re-measured: spec line 145 says schema.ts *declines* to force chose_instead for wrong/stale, which `src/corrections/schema.ts` does (superRefine forces only not_my_taste; its comment carries the same "widening is a migration, coerced content is permanent" rationale). Plan registration item 2 misparsed the sentence. Only the plan gets an appended correction.

## Controller findings before execution

- F1: plan Task 7 handler (plan lines 2374-2389) builds its own CorrectionRow literal while its comment (2392-2394) says it does not -> violates spec §2.3 "one construction point". Fixed by H1 + Task P; Task 7 must call the shared constructor.
- F2 (to check in Task 7 review): Task 7 derives projectKey from browser-supplied `body.repo` -> the request picks a filesystem path the server runs git in.

## Preflight scan (table: preflight-scan.md, sonnet seat, 192,274 tokens / 27 tool uses per dispatcher)

Controller re-verified every load-bearing row (commands -> scratchpad/scan-verify.txt):
- scan §3-4 "UNIT_SEPARATOR is an empty string" (plan L839): FALSE POSITIVE. `od -c` shows the literal is `"\037"` (U+001F); the Read tool does not render it.
- scan "throwawayStore undefined" (plan L1114): CONFIRMED, single occurrence. (It fails when the test body runs and in tsc, not at collection as the scan said.)
- scan "POST /api/reviews declared, never implemented" (plan L2269): CONFIRMED, single occurrence. Spec §4.2 table row `reviewed` requires an explicit agree/no-change click.
- scan "Task 8 uses `overall`" (plan L2588 vs its own L2482 comment): CONFIRMED.
- scan "Task 4 body still cites subdir/index.js" (plan L1665, L1669, L1700 vs L2746): CONFIRMED; disposition 9 said fixed in place, it was not.
- scan "Task 5 noSkips gate not landed" (plan L1991 still grep vs disposition 5 L2933): CONFIRMED.
- scan "TOKEN_REQUIRED produced by Task 3 Interfaces (L1030) and defined in Task 5 api.ts (L1903)": CONFIRMED.
- scan "Task 6 Files table omits src/panel/decisionSource.ts" (L2235): CONFIRMED by grep (single mention).

## Preflight rulings

- Ruling R1: Task 7 builds the row with `correctionRowFrom(input, now)` from src/corrections/record.ts; the panel has ONE clock `PanelOptions.now?: () => Date` (default wall clock), used both for correction `at` and for E2's `as_of` (this settles Task 5 decide-point 1: inject, not strip-and-compare). C-13's original landing (panel id == CLI id for the same input and clock) is restored against Task P's golden id; the named-refusal criterion stays as an extra. — why: H1 granted; spec §2.3 is binding. — cost if wrong: one extra option field on PanelOptions.
- Ruling R2: plan disposition "登记而不修" item 2 (spec §3.1 ERRATUM) is withdrawn (H2). — cost if wrong: spec keeps an ambiguous bare `schema.ts`.
- Ruling R3: Task 7 must also implement `POST /api/reviews` (action `reviewed`, explicit agree), write failure surfaced to the person (spec §4.3.1), with a criterion and a named mutation. — why: spec §4.2 names it as one of the two producers of the coverage numerator. — cost if wrong: one extra endpoint.
- Ruling R4: Task 3 `security.test.ts` defines `throwawayStore` as a mkdtemp dir owned by that test and removed after it; the child env points ORCA_CORRECTIONS_DIR there. — why: Global Constraint 2 / Rule 17; the plan's own comment says isolation must not depend on the guard. — cost if wrong: none beyond a temp dir.
- Ruling R5: `TOKEN_REQUIRED` has one definition, in `src/panel/rejection.ts` (Task 3); Task 5 api.ts imports it. — why: disposition 9's own diagnosis (two definitions = fields.ts's shape). — cost if wrong: an import path.
- Ruling R6: Task 4's prose about `subdir/index.js` (L1665, L1669, L1700) is superseded by mutation table L2746; not carried as a prediction or into the commit message.
- Ruling R7: Task 5's skip gate becomes the criterion `tests/panel/noSkips.test.ts` (disposition 5), covering it/test/describe .skip and .todo plus xit/xtest/xdescribe; comments stripped before matching; it carries must-catch AND must-not-catch samples. — why: vitest exits 0 on skipped/todo; a scanner without negative samples proves nothing. — cost if wrong: one test file.
- Ruling R8: Task 6 Files table gains Create `src/panel/decisionSource.ts`.
- Ruling R9: Task 8 MetricsView reads the real field from src/metrics/types.ts (`rate_excluding_stale` per the plan's own L2482 note; re-measure at Task 8), never `overall`.
- Ruling R10 (default for Task 8 decide-point 2, may be revised with evidence at Task 8): web keeps its own field list with its own golden, AND a root-side criterion imports both lists and asserts equality, so one mutation to either side goes red. — why: web tsconfig cannot import src; a parity test is the smallest single landing point. — cost if wrong: a second definition held together by a test instead of an import.
- Ruling R11: Task 9 edits the verify string Task 1 wrote into the target state (replace, not append).
- Ruling R12: `detailUrl` lives in `src/panel/listProjection.ts`; how verify-panel.mjs imports it is decided at Task 9 with evidence (preference: run it through tsx so there is one definition).
- Ruling R13: registered items 6 (two spec requirements without criteria: §4.2 unreviewed-high-tier todo list -> Task 8; §3.3 --bind help sentence -> Task 1), 7 (lsof filtered by pid -> Task 3), 8 (C-5 positive observation is readdir(decisionsDir), not git status -> Task 7) are carried into those tasks' dispatches.
- Ruling R14: red counts in the plan are hypotheses. After each task's review, a separate verifier subagent runs the named mutations in clones and reports measured reds; a mismatch is investigated with the three questions (earlier assertion short-circuits / who else walks the deleted line / which field a literal comes from), never auto-declared a false red.

## Tasks

- Task P (pre-slice, H1): implemented 297a2ce (BASE a801200); implementer reports whole repo 83/471, scheduler 51/167, VERIFY_RC=0. Review + mutation verification dispatched in parallel.
- Task P mutations (task-P-mutations-report.md, sonnet verifier, 98,158 tokens / 19 tool uses per dispatcher; controller read the report whole): baseline 14/88 green. MP-1 (at ignores now) red 4, matched. MP-2 (correct ignores opts.now) red 3, matched (criterion 1 green as it must be). MP-3 (default clock -> epoch) red 3, all in pre-existing record.test.ts ×2 + close.test.ts E21 — the default branch is pinned by existing criteria, not by the new file. MP-4 (by -> "panel") red 4 across injectableClock 1 & 4, record.test.ts, recordSeam.test.ts. Every red is an assertion failure with expected/received; main tree porcelain 0 bytes before and after.
- Note for Task 7: Task P golden id is `c_ed266d26d170dd27` (per MP-4 report, injectableClock.test.ts:143) — re-read it from the test file at Task 7, do not copy from here.
- Task P review (task-P-review.md, sonnet, 97,347 tokens / 13 tool uses per dispatcher): Spec ✅, quality Approved, 0 Critical / 0 Important.
- Task P: minor (deferred): record.ts moved doc comment still says "written out twice in the function below" (dangling after the move; ERRATUM present).
- Task P: minor (deferred): injectableClock.test.ts has its own recordArgs-shaped helpers next to harness.ts's runCli-shaped ones.
- Task P ⚠️ resolved by controller: ledger line 1 of .decisions/orca-dev-6354277a.jsonl is byte-identical to JSON.stringify({ev,id,at,run,...task-P-decision.json}) (scratchpad/ledger-check.mjs), committed in 297a2ce.
- Task P: complete (commits a801200..297a2ce, review clean)
- Controller docs commit eab103e: plan gains "📌 执行轮开工更正" (H1/H2 + preflight rulings R1–R14 in Chinese); ledger row orca-dev-6354277a/2 (H2) appended via appendEvent (scratchpad/append-2.mts). Porcelain 0 bytes after (/usr/bin/git).
- Checkpoint 1 (2026-09-11): stopped before Task 0/1 to report cost to the human (hook reported session ~$63.73) — CLAUDE.md/handoff practice of re-confirming budget mid-implementation overrides the skill's continuous-execution default.
- Next: Task 0 + Task 1 in one dispatch (Task 0 is measurement only and feeds Task 1's commit message).
- Checkpoint 1 answered by human (2026-09-11): continue Task 0/1; on problems follow controller recommendation; report at the end; then update the three handoffs.
- Task 0+1: dispatched one implementer (sonnet), BASE eab103e, controller rulings in task-1-controller-notes.md (C1–C9: edit-not-overwrite package.json; React 19 JSX import; vitest/config defineConfig; zero-test web check -> real smoke test not passWithNoTests; root tsc/vitest isolation from web/; §3.3 help-sentence criterion (R13); W-1 left to the verifier; commit-message fact check; final checks).
- Ruling R15: Task 0 "changed" items no longer stop for the human mid-round (human ruling 2026-09-11); only a change that affects what Task 1 builds stops the implementer. — cost if wrong: a spec drift reaches the end-of-round report instead of the start.
- Context note: hook reported ~328k context before this dispatch (Rule 6 session cap 450k) -> this session ends after Task 1's review + mutations, then handoff.
- Task 0+1 implemented 29c0de4 (BASE eab103e), DONE_WITH_CONCERNS (implementer 191,609 tokens / 100 tool uses per dispatcher): whole repo 85/477, scheduler 51/167, web check 1/1, VERIFY_RC=0; porcelain 0 bytes (/usr/bin/git); ~/.orca absent; web/dist absent (root .gitignore `dist/` will cover it); web/node_modules exists separately (vite, esbuild).
- Ruling R16: keep 29c0de4's commit trailer as written (implementer used a Sonnet attribution instead of the controller-given one) — no amend (global rule prefers new commits; the trailer names the model that wrote it). — cost if wrong: one commit's attribution line differs from the session's other commits.
- Ruling R17: `npm install` reported 5 vulnerabilities (3 moderate / 1 high / 1 critical per implementer), not triaged this round; registered for the handoff. — cost if wrong: a known-vulnerable dependency ships in the panel before someone audits.
- Task 1 review (sonnet) and mutation verifier (sonnet, W-1..W-8, task-1-mutations-brief.md) dispatched in parallel on 29c0de4.
- Task 1 review (task-1-review.md, sonnet, 104,934 tokens / 26 tool uses per dispatcher): Spec ✅, quality Approved, 0 Critical / 0 Important. ⚠️ runtime numbers not re-run (covered by implementer's verify + verifier).
- Task 1: minor (deferred): tests/panel/workspace.test.ts:46-52 lockfile-absence criterion is green before Task 1 (brief-prescribed); W-2 mutation measures whether it can go red.
- Task 1: minor (deferred): web `check` criterion asserts only that the script is a string (brief-prescribed).
- Task 1: finding for handoff: two vite copies — root node_modules/vite 5.4.21 (via vitest 2.0.5) and web/node_modules/vite 6.4.3; this is why web/vite.config.ts had to import defineConfig from vitest/config (C3).
- Task 1 mutations (task-1-mutations-report.md, sonnet, 118,529 tokens / 42 tool uses per dispatcher; controller spot-checked RC/summary lines): baseline root 3/20 + web 1/1 green. W-1, W-2 (proves the lockfile criterion can go red), W-3, W-7 matched; W-4/W-5 red in tests/panel/usage.test.ts; W-8 red on the literal ["web"]; W-6 (delete the stub's --by guard) FULLY GREEN. Main tree porcelain 0 before/after.
- Task 1: parked — W-6: the `--by` guard in the Task 1 stub src/panel/server.ts has no criterion. Ruling R18: real, deferred to Task 3, which replaces server.ts wholesale and owns the --by criteria; Task 3's dispatch MUST carry a named mutation that deletes the --by guard and must see it red (CLI level, so it survives later rewrites). — cost if wrong: the guard is unpinned between 29c0de4 and Task 3's commit (no user of the stub exists).
- Task 0: complete (measured table in task-1-report.md; decisions 148 vs spec's 137 — expected drift; nothing else changed)
- Task 1: complete (commits eab103e..29c0de4, review clean, 1 parked)
- End of session (2026-09-11 10:11): ls-remote -> eab103e (human pushed mid-session: Task P and the plan correction are now published); final verify VERIFY_RC=0, whole 85/477, scheduler 51/167, web 1/1; porcelain 0; ~/.orca absent. Controller read only summary/failure lines of the 1343-line final verify (context budget).
- Handoffs updated: ccloop Orca section and ccmem §15 in place (no new numbered items; head sha unchanged), Orca handoff appended section for session 6354277a.
- NEXT: resume at Task 2. Carry R18 (--by guard mutation) into Task 3; R4/R5 into Task 3; R6 into Task 4; R1/R5/R7 into Task 5; R8/R12 into Task 6; R1/R3/R13(C-5) and F2 into Task 7; R9/R10/R13(todo list) into Task 8; R11/R12 into Task 9. Plan's own five rulings go to the ledger at their tasks.

## Session 2 — run `orca-dev-5d7759dc` (2026-09-11, controller Opus 5 1M), resumed at Task 2

Everything above this heading was written by session `6354277a` and is left byte-for-byte alone (CLAUDE.md Rule 13).

### Start-of-session measurements (redirected to files, read back; `/usr/bin/git` everywhere)

- `git ls-remote origin refs/heads/main` -> `eab103e`; local HEAD `f7b11ba` (Task 1's commit `29c0de4` and the handoff commit are NOT published).
- `rtk proxy npm run verify` -> VERIFY_RC=0; whole repo 85 files / 477 tests, verify:scheduler 51 / 167, `@orca/web check` 1 / 1. Matches the previous session's closing numbers.
  ⚠️ The log is 1356 lines; the controller read summary + failure-keyword lines only (context budget), not the whole file. Registered, not hidden.
- `git status --porcelain -z` -> 0 bytes; one worktree. `ls ~/.orca` -> absent (Rule 17 still live).
- Controller context at start: 170,888 tokens used per the harness counter (Rule 6 cap 450k) -> this session plans Task 2 and Task 3, then hands off at a task boundary.

### Human ruling this session (2026-09-11)

- H3: proceed with the controller's proposed order (Task 2 then Task 3, brief -> controller notes -> implementer -> reviewer -> separate mutation verifier); decide problems on the controller's recommendation mid-round and report at the end; update the Orca / ccloop / ccmem handoffs when the round's work is done, keeping the sister repos' Orca sections from growing new numbered items, and writing no hard HEAD values.

### Task 2 dispatch

- BASE `f7b11ba`. Brief `task-2-brief.md` (428 lines, extracted by scripts/task-brief). Controller notes `task-2-controller-notes.md` (D1-D9).
- Ruling R19: the brief's `reviewsStore.ts` comment promises a directory chmod decided from mkdir's return value, and the code block has no such chmod. Keep the code (mkdir with explicit mode, no chmod, as `src/corrections/storeLock.ts:63` does — Rule 11 conformance) and rewrite the comment to what the code does. — why: adding the chmod would make named mutation R-9b (delete `mode:` from the mkdir call) go green, i.e. a redundant guard no mutation can pin. — cost if wrong: on a umask that strips owner bits the directory is created tighter than 0700 (the corrections store has the identical exposure).
- Ruling R20: the "an ALREADY EXISTING reviews directory keeps its own mode" criterion, which the plan registers as debt owed by Task 9, is written in Task 2 instead, and adds a fifth named mutation R-9c (unconditional `chmod(this.dir, REVIEWS_DIR_MODE)` after the mkdir) for the verifier. — why: one `it` block, and this is the task that owns the code; deferred criteria for code written three tasks earlier are how gaps survive. — cost if wrong: one more criterion and one more mutation to run than the plan budgeted.
- Ruling R21: the brief's commit message is corrected before use — it claims the store "resolves through correctionsDir" (untrue at this commit; that wiring is Task 3's) and "Three named mutations, each seen red" (the implementer does not run mutations this round — R14 — and there are five). — why: a commit message is published text; a claim of red the author never saw is the exact failure R14 exists to prevent. — cost if wrong: a wordier commit message.
- Task 2 implemented `4bcaa9e` (BASE `f7b11ba`), DONE_WITH_CONCERNS (implementer sonnet, 163,565 tokens / 40 tool uses per dispatcher): whole repo 86/485 (was 85/477), scheduler 51/167, web check 1/1, VERIFY_RC=0; `ls ~/.orca` absent; implementer read the 1338-line verify log whole. Files: `src/panel/{paths,reviewsLock,reviewsStore}.ts`, `tests/panel/reviewsStore.test.ts` (8 criteria).
- Task 2 D4 first-red seen by the implementer and pasted into the report: with `recursive: true` on the lock mkdir, `refuses by its OWN name when its OWN lock is held` failed with `promise resolved "'written'" instead of rejecting` — an assertion failure, and only that criterion. Removing `recursive` made all 8 green.
- Task 2 D8: the measured collection error is vite's `Failed to load url ../../src/panel/paths.js … Does the file exist?`, not the brief's predicted `Cannot find module`. Wording only; all 8 criteria failed to collect.
- Task 2 D5: the separator is written as the escape `"\x1f"` (four source characters, confirmed with `od -c`), not a raw 0x1F byte. Permitted by D5; it evaluates to U+001F.
- Ruling R23: keep `4bcaa9e`'s trailer as the implementer wrote it — `Co-Authored-By: Claude Sonnet 5` rather than the controller notes' literal `Claude Opus 5 (1M context)`. — why: the same shape as R16 last session (no amend; the trailer should name the model that actually wrote the commit, and a trailer naming a model that did not is a false statement in published text). — cost if wrong: this repository's commits name two different models across one plan's tasks.
- Ruling R24: the implementer's correction to the plan's red-count table stands and is carried into the verifier's brief — R-9 (loosen both mode constants) is predicted red in TWO criteria, not the plan's one, because the criterion added under R20 also asserts the literal `0o600` on the file it creates. — why: it is the plan's own "who else walks this line" question, answered by the grep in the report. — cost if wrong: the verifier measures one red where two were predicted and investigates, which is the procedure anyway.
- Task 2 review (sonnet) and mutation verifier (sonnet, R-6/R-7/R-9/R-9b/R-9c/R-10, `task-2-mutations-brief.md`) dispatched in parallel on `4bcaa9e`. R-9c and R-10 are new this round: R-9c decides whether R20's criterion is load-bearing, R-10 independently re-measures the lock first-red on committed code.
- Task 2 mutations (`task-2-mutations-report.md`, sonnet verifier, 92,269 tokens / 27 tool uses per dispatcher): baseline 8/8 green; **all six landed (hash changed every time) and all six were seen red, every prediction matched.** R-6 1 red (`does NOT share the corrections lock`), R-7 2 red (both dedupe criteria), R-9 2 red (modes criterion + R20's existing-directory criterion — R24 confirmed), R-9b 1 red (modes criterion only), R-9c 1 red (`leaves an already-existing directory's mode alone`) — **so R20's new criterion is load-bearing, not decorative** — R-10 1 red (`refuses by its OWN name`), independently reproducing the implementer's D4 first-red on committed code. Main tree: HEAD `4bcaa9e` throughout, porcelain 52 bytes before and after, byte-identical, listing only this ledger file; every teardown `cmp` confirmed no mutation leaked into a criterion.
- Task 2 review (`task-2-review.md`, sonnet, 110,309 tokens / 17 tool uses per dispatcher): Spec ✅, quality Approved, 0 Critical / 1 Important / 2 Minor. Important: `ReviewsWriter.append` checks `seen` before the first await and adds the key only after the write, so two concurrent appends on one instance both write. Minors: the trailer (ratified, R23) and the dirty `progress.md` (the controller's own ledger).
- Ruling R25: the Important finding enters the fix loop rather than being parked as out of scope — it is code this task wrote, and it is reachable in production, because Task 5's HTTP handlers share one `ReviewsWriter` and a double click is two concurrent requests. — why: an in-process dedupe guarantee that does not hold under concurrency is the guarantee's only failure mode. — cost if wrong: one extra criterion and one extra mutation on a path the panel may never drive concurrently.
- Task 2 fix round 1/5 (1 addressed, 0 open; commits `4bcaa9e`..`a5b55dc`): the key is claimed synchronously before the first await and released if the write throws; one new criterion fires two concurrent appends and asserts one "written", one "duplicate", and a literal row count of 1. Covering file 9/9, whole repo 86/486, scheduler 51/167, web 1/1, VERIFY_RC=0 (implementer sonnet, 228,901 tokens / 23 tool uses per dispatcher cumulative).
- ⚠️ Task 2 fix round 1 evidence gap, registered by the implementer and not hidden: **it did not witness the new criterion red on the pre-fix code** — its in-place revert experiment hit a self-inflicted syntax error and it restored from a snapshot rather than retry. The red-proof therefore rests entirely on mutation R-11 (restore the pre-fix ordering), dispatched to an independent verifier on `a5b55dc` with `task-2-mutations-brief-r11.md`. A green R-11 means the criterion is empty.
- Scoped re-review of `4bcaa9e`..`a5b55dc` dispatched (sonnet) in parallel with the R-11 verifier.
- Cost note (Rule 14, hook-reported only): the session hook reported ~$15.23 and later ~$56.27. The human ruled at the start of this round that problems are decided on the controller's recommendation and reported at the end, so this is registered rather than a stop.
- Task 2 fix round 1 re-review (`task-2-rereview.md`, sonnet, 80,776 tokens / 5 tool uses per dispatcher): the Important finding is ADDRESSED — `seen.add` sits directly after the `has` check with no await between them (`src/panel/reviewsStore.ts:71-83`), and the rollback only deletes a key this call added, because a pre-existing key short-circuits at the `has` check. No new Critical/Important breakage in the fix diff; the added outer try/catch rethrows unchanged. The re-reviewer reasoned the criterion CAN fail on the pre-fix ordering but did not measure it — that measurement is R-11's job.
- Task 2 fix round 1 mutation R-11 (`task-2-mutations-report.md`, "Fix round 1 — R-11" section, sonnet verifier, 87,342 tokens / 30 tool uses per dispatcher): **red, matched exactly.** RC=1, one failing criterion — `resolves exactly one of two concurrent appends for the same row as written, the other as duplicate` (expected `["duplicate","written"]`, received `["written","written"]`, reviewsStore.test.ts:99). The two sequential dedupe criteria stayed green, as the brief's open question anticipated: they await each append, so the race window is never entered. **This is the only red-proof that criterion will ever have, and it is load-bearing, not empty.** The verifier's first attempt was a broken run (anchor matched 0 times, hash unchanged, script exited before editing) and it said so plainly rather than counting it. HEAD `a5b55dc` and porcelain 52 bytes unchanged throughout.
- Task 2: complete (commits `f7b11ba`..`a5b55dc`, review clean after 1 fix round, 0 parked)
- Controller commit `cb6f88c`: ledger row `orca-dev-5d7759dc/1` (plan ruling 5) appended via `appendEvent` (scratchpad/append-ruling5.mts), plus Task 2's SDD evidence via `git add -f`. Briefs extracted mechanically from the plan and the review `.diff`s are left untracked, matching what was already tracked in this workspace. Full `rtk proxy npm run verify` with the new ledger row present: VERIFY_RC=0, whole repo 86/486, scheduler 51/167, web 1/1 — so the dogfood criterion that reads this repository's own `.decisions/*.jsonl` accepts the new row. `orca validate .decisions` exits 2, entirely from seven pre-existing `bound` rows in `orca-dev-09cc3ea1.jsonl` that predate `taskId`/`runId`; the verify wrapper tolerates exit 2 by design.
- Task 3 dispatched (sonnet implementer), BASE `cb6f88c`, brief `task-3-brief.md` (404 lines), controller notes `task-3-controller-notes.md` (E1-E10).
- Ruling R26 (E1): Task 3's `server.ts` is landed WITHOUT the plan's `buildApi`/`loadStaticFiles` imports — those modules arrive in Tasks 4 and 5, and copying the plan's block verbatim would fail `tsc` and take the whole verify red at this commit. Everything else in the block is kept verbatim, `ReviewsWriter` is wired now, and a one-line comment marks where each of the two wirings lands. — why: a task must leave the tree green; the plan's own Step 5 never noticed it was importing the future. — cost if wrong: Task 5 extends `createPanelServer` instead of writing it.
- Ruling R27 (E2): consequently **nothing is `it.skip`-ed**. The plan told Task 3 to skip its real-process criterion until Tasks 4/5 exist; under R26 that reason is gone, because the bind guard refuses before `listen()` and never reaches the API or the static files. — why: vitest exits 0 on a skipped criterion, which is how one sleeps through three tasks. — cost if wrong: the criterion fails for an unrelated reason and the implementer reports NEEDS_CONTEXT instead of parking it.
- Ruling R28 (E6): the real-process criterion must not swallow a failing `lsof`. The plan's `.catch(() => ({ stdout: "" }))` turns "no listener on that address" into a tautology when `lsof` is missing. A per-pid filter (registered item R13) is NOT the fix here: `execFileAsync` has already returned, so the child is gone and a pid filter would assert against an empty set by construction — that reason goes in the comment so the next reader does not "fix" it into one. — cost if wrong: the criterion fails loudly on a machine without `lsof` rather than passing silently.
- Ruling R29 (E5): `TOKEN_REQUIRED` is defined once, in `src/panel/rejection.ts`, even though its only consumer arrives in Task 5. — why: carried ruling R5; two definitions of a refusal code is the drift the preflight scan caught. — cost if wrong: one unused exported constant for two tasks.
- Task 3 implemented `eaedd86` (BASE `cb6f88c`), DONE (implementer sonnet, 144,029 tokens / 53 tool uses per dispatcher): `security.test.ts` went 0 → 7 passing (first-red witnessed by the implementer); whole repo 87/493, scheduler 51/167, web 1/1, VERIFY_RC=0; nothing dirty but the controller's ledger. Files: `src/panel/{token,bindGuard}.ts`, rewritten `src/panel/server.ts`, two codes added to `rejection.ts`, `tests/panel/security.test.ts`.
- Task 3 implementer's registered items: P-2 (`server.address().address`) stays unclosed as the plan registered, because under R26 nothing drives the real HTTP surface yet; and it flagged one judgement call of its own — a post-hoc `grep -n "Test Files"` over an already-redirected, already-read-whole verify log, purely to find line numbers. Rule 14 forbids filtering the measurement, not indexing a captured artefact afterwards; registered rather than waved away, because the implementer was right that the rule's text has no carve-out.
- Task 3 review (sonnet) and mutation verifier (sonnet, P-1/P-10/P-10b/P-11/P-12/P-13, `task-3-mutations-brief.md`) dispatched in parallel on `eaedd86`. P-10b is the carried R18 mutation: it deletes the `--by` guard that Task 1 shipped unpinned, and a green there is the finding this seat exists to produce.
- Task 3 review (`task-3-review.md`, sonnet, 112,342 tokens / 17 tool uses per dispatcher): Spec ✅, quality Approved, **0 Critical / 0 Important / 3 Minor** — no fix loop. The seat re-derived the load-bearing claims itself rather than trusting the report: it traced `runPanel` in `src/cli.ts` to confirm P-1/P-10/P-10b would really produce the predicted exit codes and stderr, grepped `src/` and `tests/` for `it.skip`, `0.0.0.0` and duplicate `TOKEN_REQUIRED` definitions, confirmed `correctionsDir()` is side-effect-free so an env-less unit criterion cannot reach the real `~/.orca`, and read the committed message against the diff.
- Task 3: minor (deferred): `parsePanelArgs`'s `malformed-port` and `malformed-repo-argument` refusals (verbatim from the plan, reintroduced by the wholesale `server.ts` replacement) have no criterion anywhere. Carry to Task 4/5.
- Task 3: minor (deferred): E4's `throwawayStore` landed as a `describe`-level `beforeEach`/`afterEach` rather than inline per criterion; isolation is preserved and the deviation is disclosed. The controller note predated E3's second real-process criterion, which is what made sharing it the smaller shape.
- Task 3: minor (deferred), **and a live hazard for the verifier**: under P-10 and P-10b the CLI-level criterion runs to its 20s timeout with the child still listening on loopback, and nothing kills it. The mutation run may leave a stray `tsx` process behind; the verifier was asked to report what it saw. Task 4/5 should give that criterion an explicit teardown.
- Task 3 mutations (`task-3-mutations-report.md`, sonnet verifier, 131,246 tokens / 93 tool uses per dispatcher): **all six landed (every hash differed) and all six were seen red, every prediction matched.** P-1 2 red (the real-process criterion reddened through exit 3 / EADDRNOTAVAIL rethrow, as predicted); P-10 2 red; **P-10b 2 red — the carried R18 hole is CLOSED**: Task 1's identical guard could be deleted with everything staying green, and this task's CLI-level criterion now catches the deletion; P-11 1 red; P-12 1 red (the `/^[0-9a-f]{64}$/` shape assertion stayed green exactly as predicted — only the inequality between two mints catches a fixed literal); P-13 1 red (the negative control, and only it). No compile or collection errors; every red was an assertion failure. `security.test.ts` was byte-identical in every clone (`cmp` each time). HEAD `eaedd86` and porcelain 52 bytes unchanged throughout, and `ls ~/.orca` was rechecked after every single mutation — absent all six times.
- ⚠️ Registered for Task 4/5 (measured, not theorised): P-10 and P-10b leave the real `tsx src/cli.ts panel` child alive past vitest's 20s timeout — the timeout fails the criterion but does not kill the child. The verifier found and killed one by hand on each of those runs; they only ever bound loopback. **That criterion owes an explicit teardown.**
- Task 3: complete (commits `cb6f88c`..`eaedd86`, review clean, 3 minors deferred, 0 parked). R18 discharged.
- Ruling R22: the ledger row for the plan's own ruling 5 (reviews use mkdir for the lock) goes into `.decisions/orca-dev-5d7759dc.jsonl` via appendEvent, written by the controller, not by the implementer, and not appended to run `3e50f2cb`'s file even though that run made the judgement. — why: the plan says `<本会话>` verbatim, and A' §3.1's "structurally impossible to conflict" guarantee rests on each run writing only its own file; the evidence array names the plan line and the originating run. — cost if wrong: the row is attributed to the run that landed it rather than the run that decided it, which the evidence field spells out either way.

## Session 3 — run `orca-dev-bad904b1` (2026-09-15, controller Opus 5 1M), resumed at Task 4

Everything above this heading is left byte-for-byte alone (CLAUDE.md Rule 13).

### Start-of-session measurements (redirected to files, read back; `/usr/bin/git` everywhere)

- `git ls-remote origin refs/heads/main` -> `eab103e`; local HEAD `e4e2ad6` (Task 1..3 commits and both handoff commits are NOT published).
- `rtk proxy npm run verify` -> VERIFY_RC=0; whole repo 87 files / 493 tests, verify:scheduler 51 / 167, `@orca/web check` 1 / 1. Matches Session 2's closing numbers.
  ⚠️ The log is 1349 lines; the controller indexed summary/failure lines of the captured file with a python pass (no skipped/todo/FAIL lines found), not a whole read. Registered, not hidden.
- `git status --porcelain -z` -> 0 bytes; one worktree. `ls ~/.orca` -> absent.
- Controller context at start ~141k tokens per the harness counter (Rule 6 cap 450k) -> this session plans Task 4 and Task 5, then hands off.

### Correction to Session 2's handoff (not an edit of it)

- The Orca handoff section for session `5d7759dc` says Session 2 "ends with a NEXT line naming what each task carries". Measured: `grep -n NEXT progress.md` -> one hit, line 88, in Session 1 (written before Task 2). Session 2 has no NEXT line; its last line is R22, appended after `Task 3: complete`. Line 88's carries for Tasks 4-9 are still valid; the debts Session 2 added (real-process teardown, malformed-* criteria, P-2) were never folded into a NEXT line. This session writes a fresh one at its end.

### Human ruling this session (2026-09-15)

- H4: the controller's plan is approved as listed (start checks; Task 4 then Task 5 through brief -> controller notes -> implementer -> reviewer -> separate mutation verifier; the teardown and malformed-* debts ride Task 4; report cost after Task 4's review; update three handoffs at the end; no push).

### Task 4 dispatch

- BASE `e4e2ad6`. Brief `task-4-brief.md` (288 lines, extracted by scripts/task-brief). Controller notes `task-4-controller-notes.md` (F1-F6 + mutation table S-12..S-18, M-1, M-2, T-1).
- Preflight (controller, measured into scratchpad/t4-pre.txt): the brief's only import `PanelRejection` from `./rejection.js` exists; `web/dist` does not exist; `src/panel/server.ts:93-95` says Task 4 wires `loadStaticFiles`; plan L1893 shows `StaticFiles` is consumed through Task 5's `ApiDeps`; plan L2746 already records that the brief's `subdir/index.js` rationale is wrong, and it predicts `index.js/` hits under S-12 (controller doubts it: a trailing slash on a regular file should be ENOTDIR — left to measurement).
- Ruling R30 (F1): Task 4 does not wire `loadStaticFiles` into `createPanelServer`; it lands the module and its criteria and corrects the one server.ts comment that says otherwise. — why: the only reader of the loaded files is Task 5's static route; loading now buys a dead value and makes every `orca panel` start fail on the missing `web/dist`, changing what Task 3's real-process mutations observe. — cost if wrong: Task 5 adds one line to wire it.
- Ruling R31 (F2): the brief's commit message is corrected before use — no `subdir/index.js` as the catching entry, no claim that `/etc/passwd` is reached, and mutation reds phrased as predictions the verifier measures. — why: published text; R14/R21 shape. — cost if wrong: a wordier commit message.
- Ruling R32 (F3): three criteria the brief does not have — anchor-missing refusal, non-ENOENT errors not relabelled as `panel-dist-missing`, unknown extension served as `application/octet-stream` — each with its own named mutation (S-15, S-17, S-18). — why: Rule 9, every branch gets a mutation that deletes only itself; the brief implements all three branches and pins none. — cost if wrong: three small criteria.
- Ruling R33 (F5): the real-process criteria get a per-run deadline shorter than vitest's timeout that kills the whole process tree, plus an afterEach kill; the verifier re-runs P-10/P-10b (T-1) and counts leftover processes. — why: Session 2 measured one live loopback listener left behind per mutation run; tsx 4.23.13 runs the script in a child node process, so killing the tsx pid alone can orphan the listener. — cost if wrong: more fixture code in security.test.ts.
- Ruling R34 (F6): `malformed-port` / `malformed-repo-argument` criteria go in at `parsePanelArgs` level with `--by` supplied and a negative control each, NOT through the real process. — why: on the run where the `--repo` check is deleted a real-process criterion would start a loopback server and hang, the hazard R33 repairs; and without `--by` the identity refusal would fire first (external review C2's shape). — cost if wrong: these two refusals are pinned below the CLI, so a future rewrite of the CLI's argv mapping could drop them unseen.
- Task 4 implemented `de25a8b` (Part A, staticFiles) + `def1204` (Part B, teardown + malformed-* criteria), BASE `e4e2ad6`, DONE (implementer sonnet, 181,790 tokens / 52 tool uses per dispatcher): whole repo 88/503 (was 87/493), scheduler 51/167, web 1/1, VERIFY_RC=0; porcelain 52 bytes (progress.md only, measured by controller with /usr/bin/git); `~/.orca` absent; no leftover tsx/panel process. Implementer's F5 teardown (detached spawn, negative-pid SIGKILL on a 5s deadline, reject on hang, afterEach backstop) was exercised on the happy path only — its behaviour under P-10b is not witnessed; that is T-1's job.
- Task 4 review (sonnet) and mutation verifier (sonnet, `task-4-mutations-brief.md`: S-12..S-18, M-1, M-2, P-10b, P-10 with process census) dispatched in parallel on `def1204`. Review package `review-e4e2ad6..def1204.diff`.
- Task 4 review (`task-4-review.md`, sonnet, 117,774 tokens / 18 tool uses per dispatcher): Spec ✅, quality Approved, 0 Critical / 0 Important / 2 Minor. The seat read both commit bodies via git log and confirmed F2's note states S-12 reds as predictions and drops `subdir/index.js`.
- Task 4: minor (deferred): the F6 `--port`/`--repo` criteria loop over bad values and short-circuit on the first, so M-1/M-2 prove the whole guard is pinned, not each bad value individually (implementer disclosed it).
- Task 4: minor (deferred), carried to Task 9: `loadStaticFiles` reads only the top level of `web/dist`, so a nested build output would be dropped silently. Controller measured `web/vite.config.ts:7-11` — `assetsDir: "."`, `entryFileNames: "[name].js"`, `assetFileNames: "[name].[ext]"` — so the config asks for flat output; whether a real `vite build` emits nothing nested (e.g. chunk files) is unmeasured until Task 9 builds `web/dist`. Task 9 should assert the built dist has no subdirectory.
- Task 4 mutations (`task-4-mutations-report.md`, sonnet verifier, 164,470 tokens / 109 tool uses per dispatcher; controller read the S-12 and P-10b sections whole): baseline 32/32 green in the clone; **all eleven landed (hash changed) and all eleven were seen red, every prediction matched.** S-12 red 2 — the resolving spellings were `./index.js` and `linked.txt`; `subdir/index.js` and `index.js/` did not resolve, so plan table L2746's `index.js/` claim is a false prediction (R31 kept it out of the commit). S-13..S-18 red 1 each; M-1, M-2 red 1 each; **P-10b and P-10 red 2 each, the CLI-level criterion now failing on its own 5000ms deadline (~5006ms) with the whole process group killed — census 0 surviving `tsx`/`src/cli.ts` processes on both runs. Session 2's leak is repaired and the repair is witnessed.** `~/.orca` absent before and after all 12 runs; `cmp` of both criterion files 0 every time; HEAD `def1204` and porcelain 52 bytes unchanged.
- ⚠️ Registered: the P-10b section quotes one failure message carrying both runs' pids ("pid 82647 [P-10] / 80892 [P-10b]") — the verifier merged two messages into one quote. Cosmetic; the counts and census rows are per run.
- Task 4: complete (commits `e4e2ad6`..`def1204`, review clean, 2 minors deferred, 0 parked). Debts discharged: real-process teardown (Session 2 minor), `malformed-port`/`malformed-repo-argument` criteria (Task 3 minor).

### Task 5 dispatch

- BASE `def1204`. Brief `task-5-brief.md` (308 lines). Controller notes `task-5-controller-notes.md` (G1-G13 + mutation table G-11, P-2, T-8, S-12b, K-1, E-1, C-1..C-5, N-1, N-2, W-1).
- Preflight (controller, measured into scratchpad/t5-pre.txt and t5-pre2.txt): `collect(opts)` takes `now?: () => string` (src/metrics/collect.ts:60); `correctionRowFrom(input, now: () => Date)` (src/corrections/record.ts:24); `UNRESOLVED_PROJECT_KEYS` is exported from src/metrics/discover.ts:8; `readReviews`/`ReviewRow` exist in reviewsStore.ts with `projectKey`; `withCorrectionsDir` mutates `process.env` (tests/corrections/harness.ts:25). The brief's Step 1 has empty `it` bodies, redeclares `TOKEN_REQUIRED`, omits server.ts from Files, has no HTTP-level static criteria (plan L2747 says they were added — not landed), predicts P-2 red in one criterion (plan L2733 says two), and calls `parsePanelArgs([...], {})` for a server that then loads reviews from the real `~/.orca`.
- Ruling R35 (G1): Task 5 wires `loadStaticFiles` and `buildApi` into `createPanelServer` before `listen()`; criteria that start servers build a temp dist fixture. — why: R26/R30; the static route is the loaded files' only reader. — cost if wrong: none; this is where the plan put it.
- Ruling R36 (G3, settles plan decide-point 1 per R1): `PanelOptions.now?: () => Date` is the panel's one clock, adapted to collect's `() => string`; the pass-through criterion deep-equals the whole report including `as_of`/`as_of_mode`, and mutation K-1 (stop passing `now`) proves the clock is load-bearing. — why: stripping fields before comparing is how a pass-through criterion stops pinning pass-through. — cost if wrong: one option field.
- Ruling R37 (G4): two HTTP-level static criteria, sent through `node:http` with raw paths, with a positive control (`/index.js` 200 beside `/./index.js` 404). — why: WHATWG URL parsing in `fetch` collapses `.`, `..` and `%2e%2e` on the client, so a fetch-based traversal criterion would test the URL parser, not the server. — cost if wrong: a slightly longer helper.
- Ruling R38 (G6): every server a criterion starts gets a redirected `ORCA_CORRECTIONS_DIR`, including the P-2 criterion the brief wrote with an empty env. — why: Rule 17; `correctionsDir({})` is the real `~/.orca`. — cost if wrong: none.
- Ruling R39 (G10): coverage criteria pin every branch, including a projectKey-mismatch case (C-5) the brief lacks. — why: Rule 9; E2 measured that decision ids repeat across clones. — cost if wrong: one extra criterion.
- Ruling R40 (G9): `noSkips.test.ts` must scan its own file too and stay clean by constructing its samples, not by excluding itself. — why: a scanner that exempts the file holding the spellings is one edit away from exempting everything; handoff item 9 (scanners must not alarm on their own text) is solved by construction, not exemption. — cost if wrong: a slightly awkward sample table.
- Task 5 implemented `153daec` (BASE `def1204`), DONE_WITH_CONCERNS (implementer sonnet, 323,985 tokens / 75 tool uses per dispatcher): whole repo 90/519 (was 88/503; +12 metricsApi, +4 noSkips), scheduler 51/167, web 1/1, VERIFY_RC=0, no skipped/todo; `src/metrics` diff 0 bytes (controller measured); porcelain 52 bytes (progress.md only); `~/.orca` absent. Concerns: five mutations (G-11, P-2, E-1, S-12b, W-1) predicted to redden two criteria each; W-1 and S-12b redden the same two criteria; P-2 reddens the bind-default criterion through the bind guard's rejection, not through the URL assertion.
- Controller finding before review: `src/panel/coverage.ts` holds two RAW 0x00 bytes (offset 358 in a comment on line 13, offset 756 in the key template literal on line 18; python byte scan), so git classifies the file as binary — `git diff --stat` prints `Bin 0 -> 2391 bytes` and `review-def1204..153daec.diff` says "Binary files differ", i.e. the reviewer could not have read the file. Sent back to the same implementer as a pre-review fix (escape in source, zero control bytes in every touched file, new commit, no amend) before the review package is regenerated.
- Task 5 fix round 0/pre-review: `6650aab` (new commit on `153daec`, no amend). Controller re-measured: control bytes (<0x20 except tab/LF/CR) = 0 in all five touched files; coverage.ts line 18 now spells the separator as a four-character source escape (backslash, x, zero, zero), line 13 names it in words; `git diff --stat def1204 6650aab` shows `coverage.ts | 57 +` as text. Implementer re-ran tests/panel 7 files / 48 tests green (implementer sonnet, 340,341 tokens cumulative / 16 tool uses for the fix per dispatcher). ⚠️ `153daec` keeps the raw NULs in its blob forever (history is not rewritten); a diff against that commit alone still reads "Bin".
- 🔴 Measured twice this session: an escape sequence for a NUL typed into a tool call is delivered as the raw byte. (1) the implementer's Write produced the raw NULs; (2) the controller's own first attempt to append this very ledger entry, which spelled the escape literally inside a heredoc, was rejected by the Bash tool with "command contains control characters". ⇒ **never spell NUL-class escapes literally in a brief, a heredoc or a Write; name them in words, build them with chr()/String.fromCharCode, and byte-scan the result.**
- Task 5 review (sonnet) and mutation verifier (sonnet, `task-5-mutations-brief.md`, with a byte-scan-after-edit rule) dispatched in parallel on `6650aab`. Review package `review-def1204..6650aab.diff`.
- Task 5 review (`task-5-review.md`, sonnet): Spec ❌, quality Needs fixes, 0 Critical / 1 Important / 2 Minor. Important: G11 required the broken-gate criterion (tests/panel/metricsApi.test.ts ~L184) to assert status 409, `body.code === UNRESOLVED_PROJECT_KEYS` AND `"report" in body === false`; the code assertion is missing and the report's Deviations do not mention it (Rule 12). The seat re-derived G4's raw-path claim and G9's self-scan claim by hand.
- Task 5: minor (deferred): the `now` fallback expression in api.ts `currentMetrics` is hard to read.
- Task 5: minor (deferred): the 500 fallback's `String(err)` could be `err instanceof Error ? err.message : String(err)`.
- Fix round 1 held until the mutation verifier on `6650aab` finishes — its teardown `cmp`s clone criteria against the main tree, and a fix commit to metricsApi.test.ts mid-run would make that comparison fail for a reason unrelated to any mutation.
- Task 5 mutations (`task-5-mutations-report.md`, sonnet verifier; on `6650aab`): baseline 7 files / 48 tests green in the clone. **All fourteen landed and all fourteen were seen red; no fully-green mutation.** Matched: G-11 2, T-8 1, S-12b 2, K-1 1, E-1 2, C-1 1, C-2 2, C-3 1, C-4 2, C-5 1, N-1 1, W-1 2. **Differs, both explained by question (2) "who else walks the deleted line":** P-2 red **8**, not 2 — every metricsApi criterion that starts a server through `parsePanelArgs` without `--bind` now defaults to TEST-NET-1 and trips the intact bind guard at startup; N-2 red **3**, not the implementer's 1 — `noSkips.test.ts`'s own comments (lines 20/75/88) carry literal spellings that only comment-stripping shields, so the self-scan and the directory scan redden too (the implementer's "self-scan stays green under N-2" was a false prediction; this makes comment-stripping demonstrably load-bearing on real text). No leftover process, no new LISTEN socket, no unhandled-rejection block on any run; 0 control bytes after every edit; `~/.orca` absent on every reading; HEAD `6650aab` and porcelain 52 bytes unchanged. ⚠️ The verifier's summary says "baseline + 13 mutations" while listing 14 rows — a miscount in its prose; the rows are what count.
- Task 5 fix round 1/5: `3b8bd46` adds `expect(body.code).toBe(UNRESOLVED_PROJECT_KEYS)` to the broken-gate criterion (imported constant), tests/panel/metricsApi.test.ts 12/12 green per implementer, 0 control bytes. Scoped re-review (sonnet) and a one-row verifier for new mutation E-2 (error handler answers `MetricsRejection` with a different `code`, status and body shape otherwise unchanged) dispatched in parallel. — why E-2: the broken-gate criterion's status and no-report assertions are untouched by E-2, so if that criterion reddens, the added assertion is what reddened it; without it the new line has never been seen red (Rule 9).
- Task 5 fix round 1 re-review (`task-5-rereview.md`, sonnet): finding ADDRESSED — metricsApi.test.ts:189 asserts the imported `UNRESOLVED_PROJECT_KEYS` (imported at L11 from src/metrics/discover.ts:8), after the status check and before the no-report check, so it executes; the report's "Fix round 1" section admits the Deviations omission. No new breakage (3-line test-only diff).
- Task 5 fix round 1 mutation E-2 (appended to `task-5-mutations-report.md`, sonnet verifier): **red 2, matched** — the every-request criterion at metricsApi.test.ts:151 and the broken-gate criterion at :189, both expected "unresolved-project-keys" received "mutated-code". The broken-gate criterion's first failure is exactly the assertion fix round 1 added, so that line is load-bearing. Baseline 48/48 green; hash changed; 0 control bytes; no leftover process/listener; `~/.orca` absent 4/4; HEAD `3b8bd46` and porcelain 52 bytes unchanged.
- Task 5: fix round 1/5 (1 addressed, 0 open — G11 missing `body.code` assertion; commits `6650aab`..`3b8bd46`)
- Task 5: complete (commits `def1204`..`3b8bd46`, review clean after 1 fix round plus a pre-review byte fix, 2 minors deferred, 0 parked). Carried debts discharged: P-2 criterion (Task 3 E9), R7 skip gate as a criterion, R5 single `TOKEN_REQUIRED`, R1 one clock, R26/R30 wiring.
- Controller: ledger row `orca-dev-bad904b1/1` (plan ruling 1: panel coverage alongside E2's, `src/metrics/**` untouched) appended via `appendEvent` (scratchpad/append-ruling1.mts, RC=0); new file 2813 bytes, 1 line, 0 control bytes.

### End of Session 3 measurements

- `rtk proxy npm run verify` on `3b8bd46` + the new ledger row (scratchpad/verify-end.log, 1354 lines, **read whole by the controller this time**): VERIFY_RC=0; whole repo **90 files / 519 tests**; verify:scheduler **51 / 167**; `@orca/web check` **1 / 1**; no skipped/todo in any tier. `orca validate .decisions` reports only the seven pre-existing `bound` rows of `orca-dev-09cc3ea1.jsonl` as downgraded (taskId/runId required) — the new row passes.
- `ls ~/.orca` -> absent. No surviving `src/cli.ts`/tsx process (ps census).

### NEXT (Session 3, replaces nothing above — the line-88 NEXT stays as written)

- Resume at **Task 6**. Carry into each dispatch:
  - Task 6 (decision list + detail): R8 (Files table gains `src/panel/decisionSource.ts`), R12 (`detailUrl` lives in `src/panel/listProjection.ts`); plan ruling 4 -> ledger row by the controller; routes go BEFORE the four-argument error handler in `buildApi` (G13 comment in api.ts).
  - Task 7 (record a correction): R1/R36 (use `opts.now` — the one clock — with `correctionRowFrom(input, now)`; assert against Task P's golden id re-read from tests/corrections/injectableClock.test.ts), R3 (also `POST /api/reviews`, action `reviewed`, write failure surfaced, criterion + named mutation), R13 (C-5's positive observation is `readdir(decisionsDir)`, not git status), F2 (a browser-supplied `body.repo` must not pick the filesystem path git runs in); routes before the error handler.
  - Task 8 (frontend): R9 (MetricsView reads the real field — re-measure `src/metrics/types.ts`, never `overall`), R10 (web keeps its own field list + a root-side parity criterion), R13 (unreviewed high-tier todo list); plan ruling 2 -> ledger.
  - Task 9 (success criterion): R11 (replace, not append, the verify string), R12 (import `detailUrl` through tsx), plan ruling 3 -> ledger; **new: assert the built `web/dist` has no subdirectory** (Task 4 deferred minor — `loadStaticFiles` reads the top level only).
  - Every task: every criterion that starts an in-process server uses a temp dist fixture and a redirected `ORCA_CORRECTIONS_DIR` (R38); real-process criteria reuse `runPanelProcess` from security.test.ts (R33); **never spell NUL-class escapes literally in a brief, heredoc or Write — byte-scan every touched file** (Session 3 measured it twice).
- After Task 9: final whole-branch review on the most capable model, pointed at every `minor (deferred)` line in this ledger; one fix wave; one scoped re-review. The SDD workspace is not deleted (this repo keeps it via `git add -f`).

## Session 4 — run `orca-dev-5d5c8055` (2026-09-15, controller Opus 5 1M), resumed at Task 6

Everything above this heading is left byte-for-byte alone (CLAUDE.md Rule 13).

### Start-of-session measurements (redirected to scratchpad files, read back; `/usr/bin/git` everywhere)

- `git ls-remote origin refs/heads/main` -> `a9eac55`; local HEAD `a9eac55` — **the human pushed everything Session 3 landed**; every ledger/handoff line above is now published text (corrections are appended, never edited in place). ccloop and ccmem measured the same way: remote == local, porcelain 0 bytes in both.
- `rtk proxy npm run verify` -> VERIFY_RC=0; whole repo 90 files / 519 tests, verify:scheduler 51 / 167, `@orca/web check` 1 / 1. ⚠️ The 1352-line log was indexed with a python pass for summary/RUN/skipped/todo/FAIL/Error lines (none of the bad kinds found), not read whole. Registered.
- `git status --porcelain -z` -> 0 bytes; one worktree. `ls ~/.orca` -> absent.

### Human ruling this session (2026-09-15)

- H5: the controller's plan is approved (Task 6 then Task 7 through brief -> controller notes -> implementer -> reviewer + separate mutation verifier; problems resolved by the controller's recommendation; report at the end; then update the three handoffs with no hard-coded HEAD; ccloop/ccmem Orca sections updated in place; no push).

### Task 6 dispatch

- BASE `a9eac55`. Brief `task-6-brief.md` (244 lines, scripts/task-brief). Controller notes `task-6-controller-notes.md` (H1-H12 + mutation table).
- Preflight (controller, measured into scratchpad/t6-pre.txt, t6-pre2.txt): `DecisionObservation` has all six LIST_FIELDS (src/metrics/types.ts); `readLedgerLeniently` exported (src/metrics/lenientRead.ts); `Observations.repos` carries `{projectKey, path}`; `acquireReviewsLock` waits `REVIEWS_LOCK_TIMEOUT_MS` = 1000 then throws `PanelRejection(reviews-store-busy)`; the error handler maps `PanelRejection` to 409; `acquireStoreLock` exported from src/corrections/storeLock.ts; metricsApi.test.ts's `get`/`rawGet`/`makeDistFixture` are file-local (not exported). The brief has two empty `it` bodies, writes `opened.at` with `new Date()` (violates R1/R36), pins no projectKey half of the joint key, and writes its survey to `/tmp/who6.txt`.
- Ruling R41 (H1): `opened.at` comes from `deps.opts.now` (the one clock), with a criterion asserting the fixed clock's ISO string and a named mutation K-6. — why: R1/R36; a second wall-clock read is a second clock. — cost if wrong: one assertion.
- Ruling R42 (H2): the detail endpoint answers only decisions that are in `currentMetrics(opts).observations.decisions` for the requested `(projectKey, id)`, and reads the whole ledger row from that repo's path in `observations.repos`. — why: spec §5 re-evaluates the gate on every request (a detail served past a broken gate is partial data); the list and the detail then agree on membership; a browser-supplied projectKey only ever selects among discovered repos, never a filesystem path (F2's shape). — cost if wrong: one extra `collect` per detail request (measured 2.4 ms for 144 lines in E2).
- Ruling R43 (H3): the two empty `it`s are written in full — corrections-lock-held detail returns 200 with the decision (mutation R-6b: the detail handler takes the corrections store lock); unknown id AND known id under an unconfigured projectKey both answer 404 `decision-not-found` and record nothing (mutation D-1). Plus two criteria the brief lacks: same decision id in two repos returns each repo's own row (mutation D-2: lookup ignores projectKey), and a broken gate answers the detail with 409 `UNRESOLVED_PROJECT_KEYS` (mutation D-3: detail bypasses currentMetrics). — why: Rule 9; E2 measured decision ids repeating across clones. — cost if wrong: four criteria.
- Ruling R44 (H4): the plan's `serves the detail on time even when the reviews lock is held` is merged with the `opened`-write-failure branch: hold the reviews lock past its 1000 ms budget, assert 200 + the decision first, then assert a stderr warning naming the decision appears and no `opened` row for it exists; release in finally. Mutation O-1: the `.catch` body swallows silently. — why: the `.catch` branch is otherwise unpinned, and a separate criterion would make W-6b red two criteria. — cost if wrong: one slower criterion (~1.1 s).
- Ruling R45 (H5): every `it` starts its own server with its own redirected `ORCA_CORRECTIONS_DIR`, own dist fixture and own target repo(s), closed in `finally` (R38). — why: a shared store is how L-3 reddens two criteria. — cost if wrong: slower file.
- H6 (human, mid-turn 2026-09-15): run ALL remaining tasks (6, 7, 8, 9) before stopping — not stop after Task 7. This supersedes the "two tasks per session" line of the handoff for this session.
- Ruling R46: the controller will cross CLAUDE.md Rule 6's 450k session context cap to honour H6 rather than hand off mid-plan; it keeps its own context lean (artifacts as files, reports read by section) and surfaces the breach in the final report and handoff. — why: the human's explicit instruction; Rule 6 says surface, not silently overrun. — cost if wrong: a compaction mid-plan; the ledger is the recovery map.
- Task 6 implemented `ea8c2bb` (BASE `a9eac55`), DONE (implementer sonnet, 234,424 tokens / 76 tool uses per dispatcher): whole repo 91/527 (was 90/519; +8 in the new tests/panel/decisionsApi.test.ts), scheduler 51/167, web 1/1, VERIFY_RC=0; src/metrics diff 0 bytes; no `Bin` in diffstat; control bytes 0 in all four touched files (controller re-measured with python); `~/.orca` absent. Disclosed: criteria and code written together, red measured afterwards in a throwaway clone at BASE (collection failure on the missing module — a load failure, not an assertion red); helpers copied into decisionsApi.test.ts rather than shared (metricsApi.test.ts untouched). Implementer predicts D-2 red 2 (agrees with the verifier brief).
- Task 6 reviewer (sonnet, `reviewer-instructions.md` = the skill template + project additions, written once for this session) and mutation verifier (sonnet, `task-6-mutations-brief.md`: L-3, L-3b, W-6b, O-1, K-6, R-6b, D-1, D-2, D-3) dispatched in parallel on `ea8c2bb`. Review package `review-a9eac55..ea8c2bb.diff`.
- Task 7 controller notes `task-7-controller-notes.md` written ahead (J1-J8, rulings R47-R50 recorded at Task 7 dispatch); Task 8/9 briefs extracted (`task-8-brief.md` 188 lines, `task-9-brief.md` 386 lines).
- Task 6 review (`task-6-review.md`, sonnet): Spec ✅, quality Needs fixes, 0 Critical / 2 Important / 2 Minor. Named risks (a)-(e) all checked clean. Important I-1 (plan-mandated, kept by ruling H8): with the expected rows built independently from LIST_FIELDS, the per-row `Object.keys` assertion can never redden on its own — the deep equality above it fires first — while its comment calls it "THE load-bearing assertion". Important I-2: commit `ea8c2bb`'s message says the Object.keys assertion "actually catches" L-3b; the implementer's own report says `toEqual` fires first.
- Ruling R57 (on I-1, against plan text L2104-2106 and my own H8): replace the two list assertions with ONE `toStrictEqual(expected)` (strict also refuses an extra key whose value is undefined, the only case the keys check could add over `toEqual` — and JSON over HTTP cannot carry undefined anyway); rewrite the comment to say the Object.keys line was dropped because it could not redden independently once the tautology was repaired. — why: Rule 9 (a criterion never able to go red is not a criterion) and Rule 12 (a comment claiming load-bearing that measurement contradicts); the plan's "do not de-duplicate" warning was written against the tautological form. — cost if wrong: one assertion of defence in depth removed; L-3b still has a landing (the verifier's measurement decides).
- Ruling R58 (on I-2): `ea8c2bb`'s message is not rewritten (no amend; new commits only); the fix commit's body carries a named correction of that sentence, and this ledger line records it. — cost if wrong: the wrong sentence stays in `git log` beside its correction.
- Task 6: minor (deferred): `get`/`makeDistFixture` helpers duplicated between metricsApi.test.ts and decisionsApi.test.ts (sanctioned by H7; drift risk).
- Task 6: minor (deferred): `loadDecisionRow` returns `Promise<unknown | undefined>`; a narrower type would help callers.
- Fix round 1 held until the Task 6 mutation verifier finishes (its teardown compares clone criteria with the main tree).
- Task 6 mutations (`task-6-mutations-report.md`, sonnet verifier, 178,904 tokens / 74 tool uses per dispatcher): baseline green; all nine landed (hash changed). **L-3 FULLY GREEN — a real finding**: `records NOTHING when the list is served` reads the store right after the response, while the mutated list write is fire-and-forget and lands ~500 ms later (verifier probe; 8/8 repeat runs green). Matched: L-3b 1, W-6b 1 (409 via PanelRejection), O-1 1 (stderr-warning timeout, no unhandled-rejection block), K-6 1, R-6b 1 (500 — CorrectRejection is not in the error handler's instanceof list), D-1 1, D-2 2, D-3 1. No leftover process or listener; `~/.orca` absent on every reading; porcelain 52 bytes and HEAD `ea8c2bb` unchanged; control bytes 0 after every edit.
- Ruling R59 (on L-3): "nothing was written" cannot be polled to completion, so the list criterion observes a bounded window: after its positive observation (200 and rows), `eventually(readReviews, rows => rows.length > 0, 2000)` and then assert 0 rows. Window sized from the reviews lock budget (1000 ms) plus the measured ~500 ms landing; a comment says so. — why: Rule 9, the named mutation must be seen red; the plan's "no fixed sleep" rule is about waiting for a presence, where early return is possible. — cost if wrong: ~2 s added to the green run of one criterion; a slower machine could make the window too short and the mutation green again (the verifier re-runs L-3).
- Task 6 fix round 1 dispatched to the original implementer: I-1 (R57), I-2 (R58), L-3 (R59). Covering file tests/panel/decisionsApi.test.ts.
- Task 6 fix round 1: `248f03a` (tests/panel/decisionsApi.test.ts only; implementer 8/8 in the file, tests/panel 8 files / 56 tests; 0 control bytes). Scoped re-review (`task-6-rereview.md`, sonnet, 76,423 tokens / 11 tool uses): I-1, I-2, L-3 all ADDRESSED, no new breakage. Mutation re-run on `248f03a` (appended to `task-6-mutations-report.md`, sonnet, 105,560 tokens / 51 tool uses): L-3 red 3/3 runs, each exactly the list-records-nothing criterion at its length-0 assertion; L-3b red 1 at `toStrictEqual`; porcelain 52 bytes and HEAD unchanged; `~/.orca` absent 8/8; no leftover process/listener.
- Task 6: fix round 1/5 (3 addressed, 0 open — dead Object.keys assertion, false-green L-3, commit-message mechanism; commits `ea8c2bb`..`248f03a`)
- Task 6: complete (commits `a9eac55`..`248f03a`, review clean after 1 fix round, 2 minors deferred, 0 parked). Carried R8/R12 landed; R41-R45 landed.
- Controller: ledger row `orca-dev-5d5c8055/1` (plan ruling 4: LIST_FIELDS six fields, no question/chose/because on the list) appended via `appendEvent` (scratchpad/append-row.mts + ruling4.json, RC=0); new file 2540 bytes, 1 line, 0 control bytes.

### Task 7 dispatch

- BASE `248f03a`. Brief `task-7-brief.md` (189 lines). Controller notes `task-7-controller-notes.md` (J1-J8 + mutation table C-5, C-13, K-7, E-7, C-14, M-7, V-1..V-5).
- Preflight (controller, scratchpad/t7-pre.txt): `recordNewCorrection(dir, row, {again})` and `correctionRowFrom(input, now)` and `CORRECTION_ROW_INVALID` exported from src/corrections/record.ts; `CORRECTION_ALREADY_RECORDED` from store.ts; `GOLDEN_ID` is a non-exported const at tests/corrections/injectableClock.test.ts:40; the closing path writes `.decisions/<runId>.jsonl` via appendEvents and commits (correct.ts:196-200). The brief's handler runs `projectKeyOf(body.repo)` (F2), stamps `at: new Date()` (R1), its first and third criteria send the identical request (so its C-13 prediction "only it" is false by construction), and three `it`s are empty.
- Ruling R47 (J1): the body carries projectKey + decisionId, accepted only when currentMetrics lists that exact pair; `projectKeyOf` is not used by the panel. — why: F2 (browser picks the path git runs in) and a correction with an unresolvable projectKey would make E2's gate refuse every later metrics request. — cost if wrong: the panel cannot correct a decision outside what it lists.
- Ruling R48 (J2): `correctionRowFrom(input, panel clock)` + `recordNewCorrection`; the golden-id criterion restores C-13's original landing against Task P's GOLDEN_ID (R1). — cost if wrong: one export from a test file.
- Ruling R49 (J3): the seam-refusal criterion uses an invalid kind; the empty-optional criterion keeps `chose_instead: ""` with kind wrong and checks the message names the field. — why: two identical requests make every prediction about either one wrong. — cost if wrong: none.
- Ruling R50 (J6): `POST /api/reviews` awaits the `reviewed` append and a failure reaches the person (409 via the error handler); on `POST /api/corrections` a failed `reviewed` append after a recorded correction answers 409 with `correction` in the body and a message stating the correction was recorded. — why: spec §4.3.1 (a failed `reviewed` must be seen) and Rule 12 (do not let "failed" hide "recorded"). — cost if wrong: a non-2xx on a request whose main effect happened; the body says so.
- Task 7 implemented `dc8461b` (BASE `248f03a`), DONE (implementer sonnet, 202,752 tokens / 70 tool uses per dispatcher): npm test 92/538 (was 91/527; +11 in tests/panel/correctApi.test.ts), scheduler 51/167, web 1/1, VERIFY_RC=0, no skipped/todo; red measured before implementation 11/11 at assertion level; src/metrics + src/corrections diff empty; no `Bin`; control bytes 0 (controller re-measured); `projectKeyOf` absent from src/panel. Adds `isListedDecision` shared with Task 6's detail route and a `panelClock`/`nowIso` refactor inside api.ts. GOLDEN_ID duplicated as a literal with a file/line comment (importing a `.test.ts` re-registers its describe blocks: 11 -> 15 tests, measured). Implementer corrected its own V-1 prediction to two criteria.
- ⚠️ Registered: the implementer measured its red with a temporary `git stash` in the MAIN tree, while the controller's uncommitted ledger and `.decisions/orca-dev-5d5c8055.jsonl` were present. Controller re-measured after: stash list empty, ledger intact (contains R50), `.decisions` file 2540 bytes as appended. No loss, but it touched the controller's files; later dispatches say "measure red in a clone, never stash in the main tree".
- Task 7 reviewer (sonnet) and mutation verifier (sonnet, `task-7-mutations-brief.md`) dispatched in parallel on `dc8461b`. Review package `review-248f03a..dc8461b.diff`.
- Task 7 review (`task-7-review.md`, sonnet): Spec ✅, quality Approved, 0 Critical / 0 Important / 2 Minor. Named risks (a)-(f) checked against dependency source; the GOLDEN_ID criterion's projectKey was confirmed to be what normalizeRemoteUrl derives from the fixture remote (a real cross-path equality).
- Task 7: minor (deferred): correctApi.test.ts "records another one when the page asks for it on purpose" uses the wall clock for both corrections, relying on distinct `at` values for distinct ids.
- Task 7: minor (deferred): the GOLDEN_ID criterion's sanity assertion pins `at` only; the id equality is checked once, at the HTTP layer.
- Task 7 mutations (`task-7-mutations-report.md`, sonnet verifier): baseline green; all eleven landed and all eleven seen red; no fully-green mutation. Matched: C-5 1 (fixture under `$TMPDIR/orca-target-*/repo`; this repo's HEAD and porcelain proven unchanged), E-7 1, C-14 1, M-7 1, V-1 2, V-2 1, V-3 1, V-4 1, V-5 2 (controller's count; the implementer had 1). **Differs, both explained by question (2):** C-13 red 3, not 2 — the refused-correction-writes-no-reviewed criterion also sends a bogus-kind row as its setup, and the bypass turns that refusal into a bare ZodError 500. K-7 red 2 — the wall-clock-dated correction row is later than the criteria's fixed panel clock, so E2's `future-rows-without-as-of` gate refuses every later `currentMetrics` in that criterion, including its unrelated `/api/reviews` call (reproduced twice; the count depends on today's date being after the fixed clock). Porcelain 90 bytes (controller files only) before/after; HEAD `dc8461b` unchanged; `~/.orca` absent 24/24; no leftover process or listener.
- Task 7: complete (commits `248f03a`..`dc8461b`, review clean, 2 minors deferred, 0 parked). Carried R1/R36, R3, R13 (C-5 by snapshot, not status), F2 landed; R47-R50 landed.

### Task 8 dispatch

- BASE `dc8461b`. Brief `task-8-brief.md` (188 lines). Controller notes `task-8-controller-notes.md` (K1-K9 + mutation table F-4, F-4b, F-4c, F-5, F-6, P-8, P-8b, T-1..T-4, H-1).
- Preflight (controller, scratchpad/t8-pre.txt, t9-pre.txt): `MetricsReport`/`CorrectionRate`/`RepairRate`/`METRICS_FIELDS` measured in src/metrics/types.ts (no `overall`; `rate_excluding_stale`); root tsconfig `rootDir: "."` includes `tests/**/*.ts` and `scripts/**/*.ts`; web has only App.tsx/main.tsx and one smoke test; `web/index.html` already carries `<!-- orca-panel-token -->` (= TOKEN_ANCHOR). The brief renders with `as never` in every call, keeps `overall` in its MetricsView sketch, lets `/unresolved[^<]*1/i` and `/stale/i` pass on unrelated text, gives no positive observation for the null-rate criterion, and has no server source for the §4.2 todo list (the frontend may not classify tiers).
- Ruling R51 (K2): web types live in a JSX-free web module with WEB_REPORT_FIELDS / WEB_LIST_FIELDS; the root checks set equality at runtime and mutual assignability at compile time (root typecheck). — why: R10 plus the measured fact that a field list alone lets nested types drift. — cost if wrong: a root test file importing from web/src.
- Ruling R52 (K5): the todo list is a server endpoint `GET /api/todo` computed by `unreviewedHighTier` in coverage.ts (same key and tier function as coverage); the web renders it first via a pure `PanelHome`. — why: spec §4.2's mitigation needs tier classification, and §4.1 forbids the frontend from computing. — cost if wrong: one extra endpoint and four small criteria.
- Task 8 implemented `0f506c8` (BASE `dc8461b`), DONE (implementer sonnet, 211,080 tokens / 91 tool uses per dispatcher): root 94/545 (was 92/538; +todo.test.ts, +webParity.test.ts), scheduler 51/167, web check 5 files / 9 tests (was 1/1), verify RC 0, no skipped/todo. Root tsconfig type-checks the parity file's import from web/src/types.ts (measured RC=0). Controller re-measured: control bytes 0 in every committed blob of the 15 touched files; package.json, web/package.json, src/metrics diff empty.
- 🔴 Third measured instance this plan: the implementer's first Write put a RAW NUL into web/src/DecisionList.tsx from an escape in a template literal — its own end-of-task byte scan caught it before the commit (replaced with a `::` separator). The scan rule is what caught it; the "do not type the escape" rule alone did not hold.
- Disclosed: presentational components and their criteria written together (first run green); `unreviewedHighTier` and `/api/todo` were test-first with a measured red. Implementer's F-4/F-4b/F-4c predictions 2/2/1 agree with the verifier brief.
- Task 8 reviewer (sonnet) and mutation verifier (sonnet, `task-8-mutations-brief.md`) dispatched in parallel on `0f506c8`. Review package `review-dc8461b..0f506c8.diff`.
- Task 8 review (`task-8-review.md`, sonnet): Spec ✅ (compile-time parity independently confirmed with `tsc --listFiles`: web/src/types.ts is in the root program), quality Needs fixes, 0 Critical / 1 Important / 2 Minor. Important I-1: DecisionList's React key `${projectKey}::${id}` collides for `{a::b, c}` vs `{a, b::c}`; neither field is restricted, and coverage.ts avoids exactly this with a separator that cannot appear.
- Ruling R60 (on I-1): the key becomes an exported pure `rowKey(row)` = `JSON.stringify([row.projectKey, row.id])` (injective for any two strings, no control character needed), pinned by a web criterion that the colliding pair from the review yields two different keys (plus the same row yields the same key); named mutation K-8 restores the `::` join. — why: Rule 9 (a rendered key is invisible to renderToStaticMarkup, so the function is what can be pinned) and the plan's own lesson about separators; JSON avoids typing a control byte, which has already produced raw NULs three times. — cost if wrong: one exported helper and one small criterion.
- Task 8: minor (deferred): App.tsx casts `body.decision as Decision` without validation (matches existing pattern).
- Task 8: minor (deferred): the presentational components were not individually red-then-green (disclosed by the implementer; the mutation verifier measures their criteria).
- Fix round 1 held until the Task 8 mutation verifier finishes.
- Task 8 mutations (`task-8-mutations-report.md`, sonnet verifier, 140,977+ tokens): baseline green on all four runners; all twelve landed and seen red; no fully-green mutation. Matched the implementer's predictions: F-4 2 (both on `99`), F-4b 2 (panel caveat stays green), F-4c 1, F-5 1, F-6 1, P-8 runtime 2 + both typechecks red (the web constant is typed against the web row), T-1 2, T-2 1, T-3 1, T-4 1, H-1 1. P-8b measured under two readings of "the web fixture": renaming in metricsView.test.tsx only also breaks the WEB typecheck, because web/tests/panelHome.test.tsx holds a second CorrectionRate literal nobody surveyed (question 2); renaming both fixtures gives root tsc red only, as predicted. Porcelain 90 bytes and HEAD `0f506c8` unchanged; `~/.orca` absent every reading; no process/listener left; control bytes 0 after every edit.
- Task 8 fix round 1 dispatched to the original implementer: I-1 (R60). Covering files web/tests/decisionList.test.tsx (+ web check).
- Task 8 fix round 1: `d44f6bb` (web/src/DecisionList.tsx + web/tests/decisionList.test.tsx; web check 5 files / 10 tests; root 94/545 unchanged; verify RC 0). Controller re-measured control bytes 0 in both committed blobs and the commit message. 🔴 The implementer reports TWO more raw NULs caught by its per-edit byte scan before commit (one in a doc comment describing the danger in escape syntax, one in task-8-report.md dating from the original round), and git's own commit-message control-character guard rejected a draft. ⇒ every SDD file gets a byte scan before the controller's `git add -f`.
- Scoped re-review (sonnet) and K-8 verifier (sonnet) dispatched in parallel on `d44f6bb`.
- Controller byte scan of every file in this SDD workspace: only `task-2-brief.md` (line 252) and `task-2-controller-notes.md` (line 71) hold a control byte, one each, 0x1F (U+001F, the reviews-store separator quoted from Session 2) — not NUL, so git still treats them as text. Session 2's files; left untouched (Rule 13). Every file this session wrote scans 0.
- Task 8 fix round 1 re-review (`task-8-rereview.md`, sonnet, 77,325 tokens / 10 tool uses): I-1 ADDRESSED (DecisionList.tsx:33-35, test :43-48), no new breakage. K-8 on `d44f6bb` (appended to `task-8-mutations-report.md`): red 1 — the rowKey criterion at its different-keys assertion (`'a::b::c'` vs `'a::b::c'`), matched; root typecheck 0; porcelain 90 bytes and HEAD unchanged; `~/.orca` absent 4/4.
- Task 8: fix round 1/5 (1 addressed, 0 open — React key collision; commits `0f506c8`..`d44f6bb`)
- Task 8: complete (commits `dc8461b`..`d44f6bb`, review clean after 1 fix round, 2 minors deferred, 0 parked). Carried R9, R10 (as R51), R13 todo list (as R52) landed.
- Controller: ledger row `orca-dev-5d5c8055/2` (plan ruling 2: renderToStaticMarkup, no jsdom/testing-library) appended via `appendEvent` (scratchpad/ruling2.json, RC=0).

### Task 9 dispatch

- BASE `d44f6bb`. Brief `task-9-brief.md` (386 lines; implementer reads its first section only). Controller notes `task-9-controller-notes.md` (L1-L7 + mutation table E-1..E-11, R-9b).
- Preflight (controller, scratchpad/t9-pre.txt): `src/cli.ts:310` prints `orca-panel ready url=<url> token=<token>`; tests/panel/reviewsStore.test.ts:64 already holds the existing-directory-mode criterion the brief's Step 1 asks for; a fresh `mkdtemp` dir measured mode 700; root tsconfig includes `scripts/**/*.ts`; `verify-scheduler.mjs` is the shape of a thin script; `web/index.html` carries TOKEN_ANCHOR. The brief's Files list names `tests/panel/endToEnd.test.ts` with no content; its step 9 has no positive control (deleting the static route leaves it green); its step 12 (`ls ~/.orca` absent) would fail `npm run verify` forever for anyone who has used `orca correct`.
- Ruling R53 (L1): no duplicate of the existing mode criterion; the verifier's R-9b measures it. — cost if wrong: none.
- Ruling R54 (L2): `scripts/verify-panel.ts` run through tsx, importing detailUrl and every compared code from their modules (R12 settled). — why: one definition, and root typecheck covers the script. — cost if wrong: verify depends on tsx (already a devDependency).
- Ruling R55 (L3): `tests/panel/endToEnd.test.ts` pins the script's own ready-line parser with must-catch/must-not-catch samples and a value assertion. — why: a success criterion that cannot read its own subject exits non-zero for the wrong reason (E2 §7.1's shape). — cost if wrong: one small file.
- Ruling R56 (L5 step 12): `~/.orca` is snapshotted before and compared after (absent stays absent; present stays byte-identical in names/sizes/mtimes), not required absent. — why: Rule 17's intent is "the criterion does not touch the real data", and an absent-only check punishes real use of `orca correct`. — cost if wrong: the check is weaker on a machine where `~/.orca` exists (a write that restores the mtime could slip by).
- Task 9 implemented `1a95e7d` (BASE `d44f6bb`), DONE (implementer sonnet, 331,885 tokens / 116 tool uses per dispatcher): package.json verify string replaced to the L4 target + `verify:panel: tsx scripts/verify-panel.ts` (controller read the diff: exactly the two inserted steps), scripts/verify-panel.ts (774 lines), tests/panel/endToEnd.test.ts; whole repo 95/550 (was 94/545), scheduler 51/167, web check 5/10, `verify:panel` RC 0 with 13 PASS lines (step 0 pre-check + 1-12), full `npm run verify` RC 0 in 45 s wall time; repeat run leaves identical ps/lsof/tmpdir listings. Control bytes 0 in all three committed blobs (controller). L1: existing mode criterion left unchanged (it chmods 0755 before append). Found and fixed during implementation: the teardown backstop attached a fresh `once("exit")` to an already-exited child and hung silently, leaving every mkdtemp dir behind — replaced by one exit promise captured at spawn. `web/dist` now exists in the main tree (ignored by root `.gitignore`; porcelain shows only the controller's two files).
- Task 9 reviewer (sonnet) and mutation verifier (sonnet, `task-9-mutations-brief.md`: E-1..E-11, R-9b) dispatched in parallel on `1a95e7d`. Review package `review-d44f6bb..1a95e7d.diff`.
- Task 9 review (`task-9-review.md`, sonnet): Spec ✅ (L1-L7 checked against source), quality Needs fixes, 0 Critical / 1 Important / 4 Minor. Important I-1: teardown failures (a failed rm of the fixture repo or store dir) are only `console.error`'d and never reach the exit code (scripts/verify-panel.ts:799-808) — the script whose first named risk is "no path leaves a temp dir" stays green when its own cleanup fails.
- Ruling R61 (on I-1): any teardown failure sets a non-zero exit code and prints `FAIL teardown: <what>` (a step failure keeps its own FAIL line first). — why: Rule 12, and teardown is part of what this criterion certifies. — cost if wrong: verify can go red on a transient rm failure, which is the point.
- Ruling R62 (on review Minor 2): the parser must-catch sample `0.0.0.0` in endToEnd.test.ts is replaced by another non-loopback spelling (e.g. `192.0.2.1`), so Global Constraint 6's "0.0.0.0 appears nowhere" stays literally true and no future grep-based guard learns an exception. — cost if wrong: none. Folded into fix round 1 because it is a constraint conformance and costs one line.
- Task 9: minor (deferred): step 11's forced-kill path rejects without awaiting the child's confirmed exit (unlike the panel child's teardown).
- Task 9: minor (deferred): scripts/verify-panel.ts is 774 lines in one file.
- Task 9: minor (deferred): an unnumbered step 0 pre-check (self-documented deviation).
- Fix round 1 held until the Task 9 mutation verifier finishes.
- Correction to R62's wording (not an edit of it): measured plan L43, Global Constraint 6 reads "the mutation column must not contain 0.0.0.0; use 192.0.2.1" — it does not say "appears nowhere". The "appears nowhere" phrasing came from this session's controller notes (task-5/8/9 notes). R62's decision stands on the notes' stricter rule, not on the plan's text.
- Task 9 mutations (`task-9-mutations-report.md`, sonnet verifier): baseline green; all twelve landed. Matched: E-1 step 2, E-2 step 4, E-3 step 5, E-4 step 6 (target HEAD moved; the verifier's first E-4 attempt was a broken mutation — its injected row failed ledger validation and was swallowed — caught with a debug driver and rerun), E-5 step 7, E-6 step 8 only, E-7 step 9 positive control, E-9 step 11 (raw EADDRNOTAVAIL, nothing listened; 192.0.2.1 on no interface per ifconfig), E-11 endToEnd missing-token criterion (verify:panel stays 0), R-9b reviewsStore mode criterion (0o700 vs 0o755). **Differs:** E-8 fails step 10 with 409, not 401-expected-got-200 — step 8's unresolvable-projectKey correction is still in the store, so without the token middleware the gate answers first (question 1). **E-10 FULLY GREEN**: this web build has no non-entry assets, so `assetsDir: "assets"` relocates nothing and dist stays flat (rebuilt and inspected). Porcelain 90 bytes and HEAD `1a95e7d` unchanged; `~/.orca` absent 26/26; no process, listener or `$TMPDIR` residue in any run.
- Ruling R63 (on E-10): not a missing criterion — the pre-check has a landing, but E-10's edit has no effect on today's build. Re-measure with E-10b: `entryFileNames: "js/[name].js"` (the entry chunk lands in a subdirectory), predicted FAIL at the pre-start flat-dist check. Registered: `assetsDir` becomes load-bearing only when web/src imports a CSS or asset file; the pre-check will see it then. — cost if wrong: E-10b is also green, which would make the flat-dist check unpinned.
- Task 9 fix round 1 dispatched to the original implementer: I-1 (R61) + R62. Covering: tests/panel/endToEnd.test.ts, `npm run verify:panel`.
- Task 9 fix round 1: `952739d` (scripts/verify-panel.ts teardown items as {what, run} with `FAIL teardown` + exitCode 1 and every item still running, `guardedRmRecursive`, a 5000 ms bound on confirming the child's exit; endToEnd.test.ts sample `0.0.0.0` → `198.51.100.1`). Implementer: endToEnd 5/5, typecheck 0, verify:panel twice 13 PASS with identical census; injected teardown fault in a clone → `FAIL teardown`, RC 1, other items ran. Control bytes 0 in both committed blobs (controller). Scoped re-review (sonnet) and E-10b + TD-1 verifier (sonnet) dispatched in parallel on `952739d`.
- Task 9 fix round 1 re-review (`task-9-rereview.md`, sonnet): I-1 and R62 ADDRESSED (verify-panel.ts:203-230, both destructive rm sites go through the guard; endToEnd.test.ts:26-30), timeout path reports and continues, no new Critical/Important.
- Task 9: minor (deferred): `guardedRmRecursive` checks `path.includes(prefix)` rather than a strict tmpdir-prefix match — safe for today's two mkdtemp call sites, permissive if reused.
- Task 9 fix round 1 mutations on `952739d` (appended to `task-9-mutations-report.md`, sonnet): baseline 13 PASS; **E-10b red** — dist listing `index.html` + `js/index.js`, `FAIL 0 web/dist has no subdirectory … [] vs ["js"]`, no panel process started (R63 confirmed: the flat-dist check is pinned); **TD-1 red** — 13 PASS then `FAIL teardown: remove the throwaway ORCA_CORRECTIONS_DIR: TD-1 injected`, RC 1, the other teardown items ran (the one injected leftover dir recorded under $TMPDIR with the script's prefix and removed). Porcelain 90 bytes and HEAD unchanged; `~/.orca` absent every reading.
- Task 9: fix round 1/5 (2 addressed, 0 open — teardown failure not reaching the exit code, 0.0.0.0 sample; commits `1a95e7d`..`952739d`)
- Task 9: complete (commits `d44f6bb`..`952739d`, review clean after 1 fix round, 4 minors deferred, 0 parked). Carried R11, R12 (as R54), Task 4's flat-dist minor landed; R53-R56, R61-R63 landed.
- Controller: ledger row `orca-dev-5d5c8055/3` (plan ruling 3: verify ordering with the web build before verify:panel) appended via `appendEvent` (scratchpad/ruling3.json).

### Final whole-branch review

- Range `a801200..952739d` (E3 execution from the Task P base). Reviewer: most capable model (fable), superpowers requesting-code-review's code-reviewer.md, pointed at every `minor (deferred)` / `parked` / `Registered` line in this ledger (collected into `final-review-deferred.txt`).
- Final review (`final-review.md`, fable, 324 lines; one focused probe run in the scratchpad with a redirected store and loopback port 0, zero residue): **ready with fixes**, 0 Critical / 4 Important / 11 Minor; deferred lines: 2 fix-before-merge, 7 already-fixed, 22 fine-to-leave.
  - I-1 `src/panel/reviewsStore.ts` dedupe key (decisionId, by, action) lacks projectKey — measured: `reviewed` on repo B for a same-id decision returns "duplicate"; B never leaves /api/todo or enters coverage, and the reviewed mark after a panel correction on B is dropped. Spec §4.3.2 spells exactly that key; every other join uses (projectKey, id).
  - I-2 POST without content-type → 500 TypeError; malformed JSON → 500 SyntaxError (measured).
  - I-3 web: every POST is `void`ed — an Agree 409 reaches nobody (spec §4.3.1); Correct posts `because: ""`, which the seam always refuses, so the button can never succeed; no refetch after Agree; the error page drops the server's code/message.
  - I-4 no Host check: measured `GET /` with `Host: evil.example` → 200 with the token, so a DNS-rebinding page gets the token and the whole API. Not among spec §3.3's registered gaps.
- Ruling R64 (I-1): add projectKey to the reviews dedupe key (and keep the U+001F join built with String.fromCharCode, never a typed escape), with a criterion that the same decision id in two projects dedupes separately; append a named ERRATUM to spec §4.3.2 (the spec text is published; its key contradicts §4.2's own joint key). — why: Rule 7, the evidence decides — a measured wrong number against a spec sentence that contradicts its own §4.2; nothing on disk depends on the old key (`~/.orca` absent, reviews.jsonl never written for real). — cost if wrong: an existing reviews.jsonl written by an earlier panel build would re-admit one duplicate per decision after restart.
- Ruling R65 (I-2 + deferred line 16 + review Minor "store-busy → 400"): body-parser failures answer 400 `panel-bad-request`; handlers read `req.body ?? {}` and refuse a non-object body by name; the 500 fallback uses `err instanceof Error ? err.message : String(err)`; a CorrectRejection whose code is the corrections store-busy code answers 409, not 400. — why: a client mistake is not an internal error, and a busy store is not a client mistake. — cost if wrong: status-code churn for a client that does not exist yet.
- Ruling R66 (I-3): the detail view gets a real correction form (kind, because, optional chose_instead omitted by the CLIENT when the box is blank — the person said nothing; the server still passes through whatever arrives), every POST's refusal is shown with the server's `code` and `message`, a 409 with `retry_field: "again"` offers "record another", and Agree/Correct refetch the todo list and metrics on success; the error page shows code and message. Pure helpers (`correctionBody(form)`, a refusal view) carry web criteria. — why: spec §4.3.1 and §4.4 make these person-facing obligations; plan ruling 2 (renderToStaticMarkup) still holds because the logic sits in pure functions. — cost if wrong: more web code than the plan sketched.
- Ruling R67 (I-4): a Host allowlist middleware registered BEFORE the static route: when bound to a loopback address, accept only Host names `127.0.0.1`, `localhost`, `[::1]` (any port); when bound externally with confirmation, also the bind address; anything else answers 403 with no token in the body. Criteria through `node:http` with a raw Host header (evil.example → 403 and body lacks the token; `127.0.0.1:<port>` → 200 positive control), plus a named ERRATUM appended to spec §3.3 recording DNS rebinding as found and closed. — why: security-sensitive defect with a measured exploit shape; a guard is additive and reversible. — cost if wrong: a person reaching the panel through an unusual loopback alias gets 403.
- Ruling R68 (deferred line 1): src/corrections/record.ts's dangling "written out twice in the function below" gets a further ERRATUM line at the end of that comment block (published text: append, do not edit in place).
- Remaining 9 review Minors are fine-to-leave for this cut and go to the handoff: vacuous per-pid lsof in step 11 and lsof as a hard verify dependency; no `--as-of` on the panel; `--dist` undocumented and `flag()` takes the next token blindly; no no-store/nosniff/x-powered-by headers; three encodings of the (projectKey, id) pair; in-flight `opened` writes can outlive `close()`; test helpers duplicated four times; git fixtures inherit global gpgsign/hooks; a `localhost` bind prints a URL `parseReadyLine` rejects.
- Final fix wave dispatched (one implementer, opus, `final-fix-notes.md`), BASE `952739d`.
- Final fix wave (implementer opus, 321,052 tokens / 105 tool uses per dispatcher): six commits `952739d`..`435f0f1` — `618d38d` F-1 (dedupe key + spec §4.3.2 ERRATUM), `fc03bfa` F-2 (400 `panel-bad-request`, store-busy 409, err.message fallback), `35e39a8` F-3 (correction form, `correctionBody`, typed POST results, Refusal/ErrorPage, kind parity), `6193738` F-4 (Host allowlist first, verify:panel step 9 covers it, spec §3.3 ERRATUM), `c4f5978` F-5 (record.ts ERRATUM line), `435f0f1` (portless `evil.localhost` added so HG-2 reddens either way). Implementer's verify: RC 0 in 42.4 s; npm test 95/561 (was 550), scheduler 51/167, verify:panel 13 PASS, web check 6/18 (was 10); repeat verify:panel clean census; `~/.orca` absent. Controller: 22 files, no `Bin`, 0 control bytes in every committed blob.
- Implementer's own calls (accepted, recorded here as rulings):
- Ruling R69: an undefined body (no content-type) is refused as not-an-object (400) instead of the notes' `req.body ?? {}`, which would have answered 404 and contradicted the notes' own criterion. — cost if wrong: none.
- Ruling R70: for a loopback bind, the Host allowlist also accepts the bind address itself (matters for 127.0.0.x other than .1, whose printed URL would otherwise 403); an IP literal cannot be rebound. — cost if wrong: none found.
- ⚠️ Registered: UI-1..UI-4's criteria were seen red only as a module-load failure at BASE, never at an assertion; the verifier's runs are the first assertion-level reds for them.
- Scoped re-review (sonnet) and mutation verifier (sonnet, `final-fix-mutations-brief.md`: RK-1, EB-1..3, UI-1..4, HG-1, HG-2) dispatched in parallel on `435f0f1`. Package `review-952739d..435f0f1.diff`.
- Final fix re-review (`final-fix-rereview.md`, opus): all four Important findings, both fix-before-merge deferred lines and the store-busy minor ADDRESSED; R69/R70 accepted on the merits; 24 Host edge cases probed (`127.0.0.1.evil.example`, `localhost.evil.example`, `evil.localhost`, userinfo, trailing dot, missing Host all refused; uppercase and `[::1]` accepted); spec ERRATA appended at section ends and cite the probe. No new Critical/Important. New Minors:
- Final wave: parked — N-1: App.tsx does not clear the previous decision before fetching the next, and DecisionDetail has no `key`, so during a selection change a Correct can post against the newly selected decision while the old one is still displayed, and typed form text carries over — Ruling R71: real and user-facing (a correction is append-only), but the skill allows one fix wave only and nothing downstream in this plan builds on it; parked and named in the handoff as the FIRST item of the next round, before anyone uses the panel on real data. — cost if wrong: until fixed, a fast click after switching rows can record a correction on the wrong decision.
- Final wave: minor (deferred): N-2 — outcome.test.tsx's criteria were only seen failing at module load at BASE (the verifier's UI-1..UI-4 runs are their first assertion-level reds).
- Final wave: minor (deferred): N-3 — a body-parser 413/415 answers 400; the message names the cause, the status does not.
- Final wave: minor (deferred): N-4 — `[localhost]:1` is accepted as a Host (not an attack path).
- Final fix wave mutations (`final-fix-mutations-report.md`, sonnet, 141,687 tokens / 70 tool uses): baseline green; all ten landed and seen red; no fully-green mutation. Matched: RK-1 2, EB-1 1, EB-3 1, UI-1..UI-4 1 each (their first assertion-level reds — N-2 discharged), HG-1 root 2 + verify:panel PASS 0-8 then FAIL 9 (evil.example 200 vs 403; clean census), HG-2 1. **Differs:** EB-2 red 2 but one of them by a 5000 ms timeout — with the non-object refusal removed, the no-content-type request reaches a pre-existing `body === undefined` early return that never responds (question 2); a mutant-only hang, not a defect at HEAD. Porcelain 90 bytes and HEAD `435f0f1` unchanged; `~/.orca` absent every reading; no leftovers.
- Final review: fix wave 1/1 (4 Important + 2 fix-before-merge + 1 minor addressed; commits `952739d`..`435f0f1`); residuals: 1 parked (N-1, R71), N-3/N-4 deferred, 9 review Minors fine-to-leave (R68 list).

### End of Session 4 measurements

- `rtk proxy npm run verify` on `435f0f1` (scratchpad/verify-end.log, 1393 lines; ⚠️ indexed with a python pass for summary/RUN/PASS/FAIL/skipped/todo/Error lines, not read whole — registered): VERIFY_RC=0, wall 42 s; typecheck ok; npm test **95 files / 561 tests**; ledger validate ok; CLAUDE.md 143/200; hooksPath ok; verify:scheduler **51 / 167**; web build ok; **verify:panel PASS 0-12**; `@orca/web check` **6 files / 18 tests**. No new tsx/cli.ts process after the run (ps before/after). `ls ~/.orca` absent. Porcelain: this ledger + `.decisions/orca-dev-5d5c8055.jsonl` only.
- Session cost per the harness hook (tool-reported, informational): last reading ~$145.51 before the final fix wave; not re-read after. Rule 6 context cap crossed by design (R46).

### NEXT (Session 4)

- **E3 plan is fully executed (Tasks P, 0-9, final review, one fix wave).** Before anyone uses the panel on real data: fix parked **N-1** (App.tsx keeps the previous decision on screen while the next loads and DecisionDetail has no `key`; a quick Correct can land on the wrong decision). One task: clear `detail` on selection, key DecisionDetail by `rowKey`, a web criterion, a named mutation.
- Registered and not done: `reviews.jsonl` has no retention policy; multi-process duplicate review rows accepted without a criterion; `npm install`'s 5 reported vulnerabilities not triaged (R17); N-3 (413/415 answer 400), N-4 (`[localhost]` Host accepted); the nine fine-to-leave review Minors listed under R68.
- After that: subsystem D, or B's follow-up; plan ruling 甲's `plan` half (needs the human's naming); ccloop's E1 I-2 + ruling 85.
- Push/merge remain the human's call; nothing was pushed this session.

### Correction to "End of Session 4 measurements" (not an edit of it)

- That section says "Rule 6 context cap crossed by design (R46)". No tool reported the controller's context size, so "crossed" is unmeasured; R46 only pre-accepted crossing it. The Orca handoff states it that way.
- Handoffs updated (run `orca-dev-5d5c8055`, 2026-09-16): Orca handoff appended a section for session 5d5c8055; ccloop's "📌 Orca 那条线" and ccmem's §15 updated in place (no new numbered items; each file's bytes before its Orca section sha256-identical before/after, measured). ccmem's reviews dedupe-key sentence corrected to include projectKey. No HEAD written in any of them. Nothing pushed.

## Session 5 — run `orca-dev-cbe8dda3` (2026-09-16, controller Opus 5 1M), the parked N-1

- Nothing above this heading is edited. Session 4's NEXT named parked **N-1** as the first thing to do before the panel touches real data; this session did that one item and stopped.
- Start-of-session measurements (each command redirected to a file and read back; the verify log was read WHOLE this time, 1400 lines, unlike Session 4's python index — registered there and not repeated here): `/usr/bin/git status --porcelain -z` 0 bytes on `main`; `ls ~/.orca` absent (RC 1); `rtk proxy npm run verify` RC 0 — typecheck ok, npm test **95 files / 561 tests**, ledger validate RC 2 (the seven pre-existing tier-0 downgrades in `orca-dev-09cc3ea1.jsonl`), CLAUDE.md 143/200, hooksPath ok, verify:scheduler **51 / 167**, web build ok, **verify:panel PASS 0-12**, `@orca/web check` **6 files / 18 tests**. All three repositories had unpushed local commits with the remote an ancestor of local (`merge-base --is-ancestor` RC 0): Orca 15, ccloop 1, ccmem 1.
- Human ruling H7 (AskUserQuestion, 2026-09-16): N-1's criteria are written BOTH ways — a pure unit on the guard itself, and a jsdom interaction criterion that drives `App`. The controller had measured first that the bug is structurally unable to redden under the existing setup (`environment: "node"` + `renderToStaticMarkup`, and `useEffect` never runs in static rendering), and put the dependency cost of the jsdom half in the question.
- Ruling R72 (controller, recorded as `.decisions/orca-dev-cbe8dda3.jsonl` row 1): R71 named two things — clear the loaded decision on a selection change, and give `DecisionDetail` a `key`. **Only the first is shipped.** Measured, not argued: a probe (temporary criterion, deleted in the same session) with an ALREADY-RESOLVED decision fetch — the case where React is most likely to batch the clear and the set into one commit — reported `PROBE same-node: false value: ""`, so the component unmounts on every switch and the uncontrolled form's DOM is rebuilt regardless. A `key` would be a guard that no mutation deleting only itself can redden; Rule 2 and this plan's own repeated finding (a guard readers learn to ignore is worse than none) decide it. Undo is one line of JSX plus one import, recorded in the ledger row.
- **A second hole R71 did not name, found while writing the criteria and closed with the same commit**: nothing ordered the `GET /api/decision` answers. Open row A, open row B, and A's slower answer repaints the page after the person moved on. `acceptArrival` (`web/src/selection.ts`) is now the one gate between a finished fetch and the screen. It compares `rowKey`s rather than the two fields separately — a fixed-separator join confuses ("a::b", "c") with ("a", "b::c"), the cross-repository confusion review finding I-1 / ruling R60 already had to fix once.
- Seen red BEFORE the fix (`rtk proxy npx vitest run tests/appSelection.test.tsx`, RC 1): 3 of the 4 App criteria, each failing at its own named assertion, none at module load. The fourth ("posts a correction against the decision that is on the screen") passed immediately and is registered as such — it is a regression guard, and mutation M-4 below is what proves it can fail.
- Mutations: four, all run by the controller in a `git clone --local` copy with the root `node_modules` symlinked, the four working-tree files copied in and proved byte-identical (`diff` 0 bytes each way, before and after every mutation). Clone baseline green at 8 files / 26 tests with the `RUN` line pointing at the copy. **Every one landed (sha before/after differ), every one was seen red, and every one of the four written-in-advance "red at X and only X" predictions was exact:**
  - **M-1** delete `setDecision(null)` from the effect → appSelection #1 (previous decision still on screen) and #3 (half-typed reason carried over), and only those. This is also the measurement behind R72: the clearing is what pins the form criterion, which is the job R71 assigned to the `key`.
  - **M-2** `acceptArrival` → `return true` → selection #2, #3, #4 and appSelection #2, and only those.
  - **M-3** `acceptArrival` compares `projectKey + "::" + id` instead of `rowKey` → selection #4 (the collision case) only.
  - **M-4** `onCorrect` builds the body from `home.todo[0]` instead of `selected` → appSelection #4 only.
- End-of-session measurements: `rtk proxy npm run verify` RC 0 — npm test **95 / 561** (unchanged), verify:scheduler **51 / 167** (unchanged), verify:panel **PASS 0-12** (unchanged), `@orca/web check` **6 / 18 → 8 files / 26 tests**. `ls ~/.orca` absent before and after every run, including every mutation run. `git status --porcelain` empty after the commit; diffstat carried no `Bin`; every file touched was byte-scanned after every edit and carried 0 control bytes.
- Dependencies: `jsdom` and `@testing-library/react` added to the **web** workspace's devDependencies (50 packages). `npm audit` totals are **unchanged at 5 (3 moderate, 1 high, 1 critical)** before and after, all of them in the pre-existing vitest/vite/esbuild chain — measured both times, not assumed. The R17 triage of those 5 is still not done.
- Commit: `fix(web): take the previous decision off the screen before loading the next`. Nothing pushed; no branch, no merge, no worktree removed.

### NEXT (Session 5)

- **Parked N-1 is closed.** The panel is clear to be used on real data as far as N-1 is concerned; nothing else in the residual list was judged a before-real-use blocker by the final review.
- Still registered and not done, unchanged from Session 4's NEXT: `reviews.jsonl` has no retention policy; multi-process duplicate review rows accepted without a criterion; the 5 reported vulnerabilities not triaged (R17); N-3 (413/415 answer 400); N-4 (`[localhost]` Host accepted); the nine fine-to-leave review Minors under R68.
- After that: subsystem D, or B's follow-up; plan ruling 甲's `plan` half (needs the human's naming); ccloop's E1 I-2 + ruling 85.
- Push/merge remain the human's call; nothing was pushed this session.

## Session 6 — run `orca-dev-4ccc0a0b` (2026-09-16, controller Opus 5 1M), three registered residuals

- Nothing above this heading is edited. Session 5's NEXT offered the backlog; the human (AskUserQuestion, 2026-09-16) picked **R17 + N-3 + N-4** as one round and left `reviews.jsonl` retention and subsystem D for their own rounds.
- Start-of-session measurements (each command redirected to a file and read back; the opening verify log was read WHOLE, 1391 lines): `/usr/bin/git status --porcelain` empty on `main`; `ls ~/.orca` absent (RC 1); `rtk proxy npm run verify` RC 0 — npm test **95 files / 561 tests**, verify:scheduler **51 / 167**, verify:panel **PASS 0-12**, `@orca/web check` **8 files / 26 tests**, CLAUDE.md 143/200, hooksPath ok, ledger validate RC 2 (the same seven pre-existing tier-0 downgrades in `orca-dev-09cc3ea1.jsonl`).
- 🔴 **Session 5's handoff said three repositories had unpushed commits. Measured at the start of this session, all three were at the same point as their remotes** — the human pushed in between. That is the third time the "unpushed expires into pushed" note has paid off, and it changed this round's comment discipline: both comment blocks touched below were already published, so each keeps its original text verbatim and carries an APPENDED named ERRATUM.
- **Ruling R73** (`.decisions/orca-dev-4ccc0a0b.jsonl` row 1): R17's five advisories (3 moderate / 1 high / 1 critical, `npm audit --json` measured 2026-09-16: `vitest` critical, `vite` high, `@vitest/mocker` / `vite-node` / `esbuild` moderate) are **not fixed**. Every one is reachable only through `vitest --ui`, a vite dev server, or the mocker — none of which this repository starts; the panel serves a built `web/dist` over `node:http`. `npm audit fix --force` is a vitest major bump for zero reachable risk. What ships instead is the **premise**, not the conclusion: `shippedAuditedPackages` in `tests/panel/workspace.test.ts` reads root + web `package.json` and `package-lock.json` offline and names any audited package a production install would pull in. `npm audit` itself is NOT the criterion — it needs the network and would redden on a remote advisory unrelated to this repository.
  - ⚠️ **Registered**: `npm audit`'s human-readable report printed only 2 advisories while its own summary said 5. The `high` and the `critical` were folded away. **A reader of the text report misses the critical one**; `--json` is the only honest read.
- **Ruling R74** (row 2): N-4's fix was written with two guards — a hex-digits/colons/dots character class AND a "must contain a colon" clause — and **the character class was then deleted**. Measured, not argued: replacing it with `[^\]]+` in the clone (sha `ebf833c990f012c3` → `192de6eadf884df9`, the mutated line read back) left `tests/panel/security.test.ts` at **11 passed (11)**. `hostnameOf`'s return value is only ever matched exactly against the loopback names and the bind address, so both malformations that matter (`[localhost]`, `[127.0.0.1]`) are caught by the colon alone. **This is R72's shape a second time, and the first time the answer was "no, it cannot redden".**
- **N-3**: the body-parser arm flattened the parser's own 413/415 into 400. `res.status(err.status)` now passes it through; the `code` stays `panel-bad-request` because nothing reads a code we would have invented and the client's remedy does not differ by status.
- **The R17 criterion is a guard that is green the day it is written**, so it ships with a **must-catch sample** beside it: a fixture where one package is a runtime dependency and another is a non-dev lock entry, asserting the predicate names exactly those two. **Seen red against a `return []` stub — and that same stub left the REAL-tree criterion green.** That false pass is the whole reason the sample exists.
- Seen red BEFORE each fix, each at its own named assertion, none at module load: N-4's criterion `expected true to be false` at `[localhost]:1`; N-3's two `expected 400 to be 413` and `expected 400 to be 415`; R17's must-catch `expected [] to deeply equal [ 'esbuild', 'vitest' ]`.
- **Mutations: six, all in a `git clone --local` copy** with the root and web `node_modules` symlinked and the five working-tree files copied in (`diff` 0 bytes each way, before and after every mutation; the clone's `package.json` and `package-lock.json` restored to 0 bytes too). Clone baseline green at 3 files / 35 tests. **Every one landed (sha before/after differ, the mutated line read back), every one was seen red, and every one of the six written-in-advance "red at X and only X" predictions was exact — not one turned out to be "at least":**
  - **M-1** drop `literal.includes(":")` → the N-4 criterion, and only it.
  - **M-2** `res.status(err.status)` → `res.status(400)` → the 413 and the 415 criteria, and only those two.
  - **M-3** delete the manifest loop in `shippedAuditedPackages` → the must-catch sample only (`['esbuild']` vs `['esbuild','vitest']`).
  - **M-4** delete the lockfile loop → the must-catch sample only (`['vitest']`).
  - **M-5** move `vitest` into the clone's runtime `dependencies` → the REAL-tree criterion only (`['vitest']` vs `[]`).
  - **M-6** flip `node_modules/esbuild`'s `dev` to false in the clone's lockfile → the REAL-tree criterion only (`['esbuild']` vs `[]`).
  - (A seventh run, before M-1, was the R74 probe: it landed and reddened **nothing**, which is what deleted the character class.)
- End-of-session measurements: `rtk proxy npm run verify` RC 0 — npm test **95 / 561 → 95 / 566** (+5: one N-4, two N-3, two R17), verify:scheduler **51 / 167** unchanged, verify:panel **PASS 0-12** unchanged, `@orca/web check` **8 / 26** unchanged. `ls ~/.orca` absent before and after every run, including every mutation run. `git status --porcelain` 0 bytes after the commit; diffstat carried no `Bin`; every file touched was byte-scanned after every edit and carried 0 control bytes. `git worktree list` shows the one main worktree, unchanged.
  - ⚠️ **Registered honestly**: the end-of-session verify log (1406 lines) was read back by an **exhaustive classifier** — every line matched against a list of known-benign patterns, with any unmatched line printed. **0 of 1406 unclassified.** That is stronger than Session 4's summary index (nothing is dropped unseen) and weaker than Session 5's line-by-line read. The opening log WAS read line by line.
- Commit: `fix(panel): let the body parser's own status reach the client, and read brackets as an IP literal`. Nothing pushed; no branch, no merge, no worktree removed.

### NEXT (Session 6)

- Still registered and not done: `reviews.jsonl` has no retention policy; multi-process duplicate review rows accepted without a criterion; N-3 and N-4 are now closed, **R17 is closed as a ruling, not as an upgrade** — if anyone ever wants the upgrade, it is a vitest major and all three baselines must be re-measured; the nine fine-to-leave review Minors under R68.
- The `reviews.jsonl` retention question writes to user-global data outside the repository (CLAUDE.md Rule 17), so it is a policy decision, not a patch: brainstorming → spec → plan, and its criteria must route through `ORCA_CORRECTIONS_DIR`.
- After that: subsystem D, or B's follow-up; plan ruling 甲's `plan` half (needs the human's naming); ccloop's E1 I-2 + ruling 85.
- Push/merge remain the human's call; nothing was pushed this session.
