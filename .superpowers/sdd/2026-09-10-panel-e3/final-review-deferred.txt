progress.md:63: - Task P: minor (deferred): record.ts moved doc comment still says "written out twice in the function below" (dangling after the move; ERRATUM present).
progress.md:64: - Task P: minor (deferred): injectableClock.test.ts has its own recordArgs-shaped helpers next to harness.ts's runCli-shaped ones.
progress.md:79: - Task 1: minor (deferred): tests/panel/workspace.test.ts:46-52 lockfile-absence criterion is green before Task 1 (brief-prescribed); W-2 mutation measures whether it can go red.
progress.md:80: - Task 1: minor (deferred): web `check` criterion asserts only that the script is a string (brief-prescribed).
progress.md:83: - Task 1: parked — W-6: the `--by` guard in the Task 1 stub src/panel/server.ts has no criterion. Ruling R18: real, deferred to Task 3, which replaces server.ts wholesale and owns the --by criteria; Task 3's dispatch MUST carry a named mutation that deletes the --by guard and must see it red (CLI level, so it survives later rewrites). — cost if wrong: the guard is unpinned between 29c0de4 and Task 3's commit (no user of the stub exists).
progress.md:98:   ⚠️ The log is 1356 lines; the controller read summary + failure-keyword lines only (context budget), not the whole file. Registered, not hidden.
progress.md:139: - Task 3: minor (deferred): `parsePanelArgs`'s `malformed-port` and `malformed-repo-argument` refusals (verbatim from the plan, reintroduced by the wholesale `server.ts` replacement) have no criterion anywhere. Carry to Task 4/5.
progress.md:140: - Task 3: minor (deferred): E4's `throwawayStore` landed as a `describe`-level `beforeEach`/`afterEach` rather than inline per criterion; isolation is preserved and the deviation is disclosed. The controller note predated E3's second real-process criterion, which is what made sharing it the smaller shape.
progress.md:141: - Task 3: minor (deferred), **and a live hazard for the verifier**: under P-10 and P-10b the CLI-level criterion runs to its 20s timeout with the child still listening on loopback, and nothing kills it. The mutation run may leave a stray `tsx` process behind; the verifier was asked to report what it saw. Task 4/5 should give that criterion an explicit teardown.
progress.md:143: - ⚠️ Registered for Task 4/5 (measured, not theorised): P-10 and P-10b leave the real `tsx src/cli.ts panel` child alive past vitest's 20s timeout — the timeout fails the criterion but does not kill the child. The verifier found and killed one by hand on each of those runs; they only ever bound loopback. **That criterion owes an explicit teardown.**
progress.md:155:   ⚠️ The log is 1349 lines; the controller indexed summary/failure lines of the captured file with a python pass (no skipped/todo/FAIL lines found), not a whole read. Registered, not hidden.
progress.md:179: - Task 4: minor (deferred): the F6 `--port`/`--repo` criteria loop over bad values and short-circuit on the first, so M-1/M-2 prove the whole guard is pinned, not each bad value individually (implementer disclosed it).
progress.md:180: - Task 4: minor (deferred), carried to Task 9: `loadStaticFiles` reads only the top level of `web/dist`, so a nested build output would be dropped silently. Controller measured `web/vite.config.ts:7-11` — `assetsDir: "."`, `entryFileNames: "[name].js"`, `assetFileNames: "[name].[ext]"` — so the config asks for flat output; whether a real `vite build` emits nothing nested (e.g. chunk files) is unmeasured until Task 9 builds `web/dist`. Task 9 should assert the built dist has no subdirectory.
progress.md:182: - ⚠️ Registered: the P-10b section quotes one failure message carrying both runs' pids ("pid 82647 [P-10] / 80892 [P-10b]") — the verifier merged two messages into one quote. Cosmetic; the counts and census rows are per run.
progress.md:201: - Task 5: minor (deferred): the `now` fallback expression in api.ts `currentMetrics` is hard to read.
progress.md:202: - Task 5: minor (deferred): the 500 fallback's `String(err)` could be `err instanceof Error ? err.message : String(err)`.
progress.md:225: - After Task 9: final whole-branch review on the most capable model, pointed at every `minor (deferred)` line in this ledger; one fix wave; one scoped re-review. The SDD workspace is not deleted (this repo keeps it via `git add -f`).
progress.md:234: - `rtk proxy npm run verify` -> VERIFY_RC=0; whole repo 90 files / 519 tests, verify:scheduler 51 / 167, `@orca/web check` 1 / 1. ⚠️ The 1352-line log was indexed with a python pass for summary/RUN/skipped/todo/FAIL/Error lines (none of the bad kinds found), not read whole. Registered.
progress.md:258: - Task 6: minor (deferred): `get`/`makeDistFixture` helpers duplicated between metricsApi.test.ts and decisionsApi.test.ts (sanctioned by H7; drift risk).
progress.md:259: - Task 6: minor (deferred): `loadDecisionRow` returns `Promise<unknown | undefined>`; a narrower type would help callers.
progress.md:278: - ⚠️ Registered: the implementer measured its red with a temporary `git stash` in the MAIN tree, while the controller's uncommitted ledger and `.decisions/orca-dev-5d5c8055.jsonl` were present. Controller re-measured after: stash list empty, ledger intact (contains R50), `.decisions` file 2540 bytes as appended. No loss, but it touched the controller's files; later dispatches say "measure red in a clone, never stash in the main tree".
progress.md:281: - Task 7: minor (deferred): correctApi.test.ts "records another one when the page asks for it on purpose" uses the wall clock for both corrections, relying on distinct `at` values for distinct ids.
progress.md:282: - Task 7: minor (deferred): the GOLDEN_ID criterion's sanity assertion pins `at` only; the id equality is checked once, at the HTTP layer.
progress.md:298: - Task 8: minor (deferred): App.tsx casts `body.decision as Decision` without validation (matches existing pattern).
progress.md:299: - Task 8: minor (deferred): the presentational components were not individually red-then-green (disclosed by the implementer; the mutation verifier measures their criteria).
progress.md:324: - Task 9: minor (deferred): step 11's forced-kill path rejects without awaiting the child's confirmed exit (unlike the panel child's teardown).
progress.md:325: - Task 9: minor (deferred): scripts/verify-panel.ts is 774 lines in one file.
progress.md:326: - Task 9: minor (deferred): an unnumbered step 0 pre-check (self-documented deviation).
progress.md:330: - Ruling R63 (on E-10): not a missing criterion — the pre-check has a landing, but E-10's edit has no effect on today's build. Re-measure with E-10b: `entryFileNames: "js/[name].js"` (the entry chunk lands in a subdirectory), predicted FAIL at the pre-start flat-dist check. Registered: `assetsDir` becomes load-bearing only when web/src imports a CSS or asset file; the pre-check will see it then. — cost if wrong: E-10b is also green, which would make the flat-dist check unpinned.
progress.md:334: - Task 9: minor (deferred): `guardedRmRecursive` checks `path.includes(prefix)` rather than a strict tmpdir-prefix match — safe for today's two mkdtemp call sites, permissive if reused.
progress.md:342: - Range `a801200..952739d` (E3 execution from the Task P base). Reviewer: most capable model (fable), superpowers requesting-code-review's code-reviewer.md, pointed at every `minor (deferred)` / `parked` / `Registered` line in this ledger (collected into `final-review-deferred.txt`).
