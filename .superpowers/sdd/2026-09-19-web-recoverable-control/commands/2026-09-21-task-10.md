# Task 10 commands and exit codes

Every command was run from the repository root on 2026-09-21. Verification commands were wrapped in
`rtk` (the local proxy); `PATH="/usr/local/bin:$PATH"` is prefixed because the homebrew node on this
machine crashes on import. Nothing below invoked an external model: the only adapter binary used was
`/tmp/ccloop-codex-0919/dist/cli.js`, whose `control capabilities` path answers from a literal and
whose worker was never started, plus `tests/control/fixtures/fake-ccloop-control.mjs`.

## GREEN (final, this round)

| # | Command | RC | Result |
|---|---|---|---|
| 1 | `PATH="/usr/local/bin:$PATH" npx vitest run tests/control/webFaults.test.ts` | 0 | 10 passed (10) |
| 2 | `PATH="/usr/local/bin:$PATH" npx vitest run tests/control/webMutations.test.ts` | 0 | 10 passed (10) |
| 3 | `PATH="/usr/local/bin:$PATH" ORCA_CCLOOP_BIN=/tmp/ccloop-codex-0919/dist/cli.js npx vitest run tests/control/webCcloopSmoke.test.ts` | 0 | 4 passed (4) |
| 4 | `PATH="/usr/local/bin:$PATH" rtk npm run verify:web-control` | 0 | 11 files passed; 112 passed / 2 skipped |
| 5 | `PATH="/usr/local/bin:$PATH" rtk env ORCA_CCLOOP_BIN=/tmp/ccloop-codex-0919/dist/cli.js npm run verify:web-control:consumer` | 0 | 1 file passed; 4 passed |
| 6 | `PATH="/usr/local/bin:$PATH" rtk npm --workspace web run check` | 0 | 11 files passed; 58 passed |
| 7 | `PATH="/usr/local/bin:$PATH" rtk env ORCA_CCLOOP_BIN=/tmp/ccloop-codex-0919/dist/cli.js npm test` | 0 | 172 files passed / 1 skipped; 1516 passed / 3 skipped |
| 8 | `PATH="/usr/local/bin:$PATH" rtk npm run typecheck` | 0 | no output |
| 9 | `PATH="/usr/local/bin:$PATH" rtk npm run build --workspace web` | 0 | `✓ built in 531ms`, 45 modules |
| 10 | `PATH="/usr/local/bin:$PATH" ORCA_CCLOOP_BIN=/tmp/ccloop-codex-0919/dist/cli.js ORCA_CCLOOP_ADAPTER_CONFIG=/tmp/orca-ccloop-d3-task8/fake-codex-config.json rtk npm run verify:control` | 0 | 39 files passed; 403 tests |
| 11 | `PATH="/usr/local/bin:$PATH" rtk npm run verify:panel` | 0 | `PASS 0`–`PASS 14` |

The two skips in row 4 are `webCcloopSmoke.test.ts`'s two real-binary tests, skipped because that
script does not export `ORCA_CCLOOP_BIN`; row 5 and row 7 run them. The three skips in row 7 are the
pre-existing `tests/control/ccloopProtocol.integration.test.ts` live-protocol cases, which stay
skipped unless `ORCA_CONTROL_VERIFY=1` (row 10 runs them: 39 files / 403 tests, no skips).

## RED (observed while writing the guards, each corrected against production, not against the test)

| Command | RC | Red assertion | Production fact it revealed |
|---|---|---|---|
| `npx vitest run tests/panel/controlRecoveryApi.test.ts` | 1 | `expected 500 to be 200` on `/api/control/runs/:runId/evidence` | the fixture had booked an `artifacts` row with no bytes on disk; `readArtifact` re-hashes the file, so a ledger row alone is a dangling reference. Fixed by archiving through `writeArtifact`. |
| `npx vitest run tests/panel/controlRecoveryApi.test.ts` | 1 | `expected 404 to be 200` on the manifest's `downloadUrl` | **no route served evidence bytes at all.** Added `GET /api/control/runs/:runId/evidence/:artifactId` in `src/panel/controlApi.ts` (production change, see final report §deviations). |
| `npx vitest run tests/panel/controlRecoveryApi.test.ts` | 1 | `expected 422 to be 202` for a start after the profile degraded | `scheduleStart` probes at command time; only a degradation that appears *after* acceptance can become a `recovery_blockers` row. Test reordered to that honest sequence. |
| `npx vitest run tests/panel/controlApi.test.ts` | 1 | `expected 'run-x' not to be 'run-x'` on the continuation run | `runViews` orders by run id (a UUID), so a continuation must be found by lineage, never by array position. |
| `npx vitest run tests/control/webFaults.test.ts` | 1 | 10/10 files-red: `importControlPlan` threw synchronously instead of rejecting | no fault seam exists in `createEstimate`/`scheduleStart`, `execution_snapshots` already holds 4 content-addressed rows after import, and `start` only arms a wake -- the claim needs `deliverScheduledStart`. |
| `npx vitest run tests/control/webMutations.test.ts` | 1 | 4 failed / 6 passed → `expected 3 to be 2`, `[{"code":"too_big","maximum":9007199254740991,"path":["payload","operations",0,"value"]}]`, `expected false to be true`, `expected 2 to be 1` | `webFixture` books the import command too; safe-integer bounds throw out of zod *before* the ledger opens; `ledger.usageUnknown` is a settlement-time projection, not a read-time derivation; `recovery/retry` is itself a durable command and advances the revision. |
| `npx vitest run tests/control/webMutations.test.ts` | 1 | `expected {tokens:3000000,…} to deeply equal {handoff:{…},work:{…}}` | the run view flattens `remaining`; the before/after comparison of the *view* is the honest way to pin the retained grant. |
| `ORCA_CCLOOP_BIN=… npx vitest run tests/control/webCcloopSmoke.test.ts` | 1 | 3 failed / 1 passed → `no such column: phase`, `ENOENT … record.json` | `runs` stores `phase` inside `body`, and the protocol fixture only records the wire when the adapter config names a `record` path. |
| `ORCA_CCLOOP_BIN=… npx vitest run tests/control/webCcloopSmoke.test.ts` | 1 | 1 failed → `expected [] to deeply equal [ {artifactId:"evidence-usage",…} ]` | the fixture's `collect` emits an event only when `collectRef` is configured -- a consumer that reports nothing is not a consumer whose evidence you can verify. |

## Log hashes (sha256, `test-logs/`)

```text
e0196bfd01ca61f328bb76fbc6889ba202256972f576d499e254f95d580c10ef  test-logs/root-suite.log
8284e4cf41b6ce8d4ef26a3c688eb3ea4294a67839788ca6a4b65f0fdf52894f  test-logs/typecheck.log
babb2c1346628bf027e78455698511edc007c39f2bcabf9ae5658009cd867609  test-logs/verify-control.log
7799ce6d8dee456507ccffa3403db80e9c940f890a2293bae331d70014b2c821  test-logs/verify-panel.log
6594e5eb69dd3691cd28506fae8a8bb64321159d0785de91a3c553122a955859  test-logs/verify-web-control-consumer.log
f199a49c18467d357f7915ddf9adacbb940558f8ff0d597d33ab97c306f47d54  test-logs/verify-web-control.log
a1edf5fd14cbc262f5da502cb207d029683600b9ca0597ae82c30bacef6e8c8d  test-logs/web-build.log
80553c4449c6867e31c6576063f90c5db19cc5abba417315f2f6f68d77206507  test-logs/web-check.log
2ee4e156006d3df00baa811ad9607f42076720860aa9844986283fea13c0f730  test-logs/webCcloopSmoke-green.log
a3341cb1d456e0c50ce19712f73b0dd87777a2f07b7ffcc5569723522a6953b7  test-logs/webMutations-green.log
```
